% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/period_levels_fill.R
\name{period_levels_fill}
\alias{period_levels_fill}
\alias{period_levels_fill_one}
\alias{period_levels_fill_five}
\alias{period_levels_fill_ten}
\title{Fill in Gaps in Period Levels}
\usage{
period_levels_fill(
  labels,
  breaks = NULL,
  interpret_single = c("lower", "upper"),
  interpret_multi = c("include", "exclude"),
  interpret_fail = c("error", "warn", "silent")
)

period_levels_fill_one(
  labels,
  interpret_single = c("lower", "upper"),
  interpret_multi = c("include", "exclude"),
  interpret_fail = c("error", "warn", "silent")
)

period_levels_fill_five(
  labels,
  interpret_single = c("lower", "upper"),
  interpret_multi = c("include", "exclude"),
  interpret_fail = c("error", "warn", "silent")
)

period_levels_fill_ten(
  labels,
  interpret_single = c("lower", "upper"),
  interpret_multi = c("include", "exclude"),
  interpret_fail = c("error", "warn", "silent")
)
}
\arguments{
\item{labels}{Vector of period labels.}

\item{breaks}{Boundaries of newly-created periods.
Boundaries for existing periods can be omitted.}

\item{interpret_single}{How to interpret
labels for single-year periods.
Choices are \code{"lower"} (the default) and \code{"upper"}.
See below for details.}

\item{interpret_multi}{How to interpret
labels for multi-year periods.
Choices are \code{"include"} (the default) and
\code{"exclude"}. See below for details.}

\item{interpret_fail}{Action if element of \code{labels}
cannot be interpreted. Choices are \code{"error"} (the default),
\code{"warn"}, and \code{"silent"}.}
}
\value{
Factor with the same length as \code{labels}.
}
\description{
Fill in gaps in levels of \code{labels}.
}
\details{
If \code{labels} is not a factor, and so does
not have levels, convert it to
a factor before filling in levels.
\itemize{
\item \code{period_levels_fill} adds periods
specified by \code{breaks}.
\item \code{period_levels_fill_one} adds periods with
width 1.
\item \code{period_levels_fill_five} adds periods with
width 5.
\item \code{period_levels_fill_ten} adds periods with
width 10.
}
}
\section{Controlling how period labels are interpreted}{


If \code{interpret_single} is \code{"lower"} (the default),
then labels for single-year periods
are assumed to refer to lower limits,
so that \code{"2025"} means \verb{[2025,2026)}.
This is the convention that data
providers typically use
for calendar years.

If \code{interpret_single} is \code{"upper"},
then labels for single-year periods
are assumed to refer to upper limits,
so that \code{"2025"} means \verb{[2024,2025)}.
This is the convention that data providers
typically use for non-calendar years,
such as 1 July to 30 June.

If \code{interpret_multi} is \code{"include"} (the default),
then labels for multi-year periods
are assumed to include upper limits,
so that \code{"2025-2030"} means \verb{[2025,2030)}.

If \code{interpret_multi} is \code{"exclude"},
then labels for multi-year periods
are assumed to exclude the upper limits,
so that \code{"2025-2030"} means \verb{[2025,2031)}.
}

\examples{
labels <- factor(c("2020-2025", "2030-2035"))
labels
period_levels_fill(labels) ## uses existing boundaries
period_levels_fill(labels, breaks = 2028)
period_levels_fill_one(labels)
period_levels_fill_five(labels)

labels <- c("2051-2061", "2021-2031")
period_levels_fill_ten(labels)

## levels are used by functions
## such as 'table()'
labels |> table()
labels |>
  period_levels_fill_ten() |>
  table()

## sort after filling
labels |>
  period_levels_fill_ten() |>
  period_levels_sort() |>
  table()
}
\seealso{
\itemize{
\item \code{\link[=age_levels_fill]{age_levels_fill()}} Age equivalent of \code{period_levels_fill()}
\item \code{\link[=cohort_levels_fill]{cohort_levels_fill()}} Cohort equivalent of \code{period_levels_fill()}
}
}
