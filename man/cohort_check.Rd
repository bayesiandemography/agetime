% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cohort_check.R
\name{cohort_check}
\alias{cohort_check}
\alias{cohort_assert}
\title{Check or Make Assertions About Cohorts}
\usage{
cohort_check(
  labels,
  no_overlap = FALSE,
  no_gap = FALSE,
  no_total = FALSE,
  no_na = FALSE,
  has_open_left = FALSE,
  has_open_right = FALSE,
  interpret_single = c("lower", "upper"),
  interpret_multi = c("include", "exclude"),
  interpret_fail = c("error", "warn", "silent")
)

cohort_assert(
  labels,
  no_overlap = FALSE,
  no_gap = FALSE,
  no_total = FALSE,
  no_na = FALSE,
  has_open_left = FALSE,
  has_open_right = FALSE,
  interpret_single = c("lower", "upper"),
  interpret_multi = c("include", "exclude"),
  interpret_fail = c("error", "warn", "silent")
)
}
\arguments{
\item{labels}{Vector of cohort labels.}

\item{no_overlap}{Check that no cohorts overlap.
Default is \code{FALSE} (don't check).}

\item{no_gap}{Check that all cohorts between
the earliest and latest cohort are
included.
Default is \code{FALSE} (don't check).}

\item{no_total}{Check that there is no "Total" label.
Default is \code{FALSE} (don't check).}

\item{no_na}{Check that there is no \code{NA} label.
Default is \code{FALSE} (don't check).}

\item{has_open_left}{Check that at least
one cohort is open on the left (has no lower limit).
Default is \code{FALSE} (don't check).}

\item{has_open_right}{Check that at least
one cohort is open on the right (has no upper limit).
Default is \code{FALSE} (don't check).}

\item{interpret_single}{How to interpret
labels for single-year cohorts.
Choices are \code{"lower"} (the default) and \code{"upper"}.
See below for details.}

\item{interpret_multi}{How to interpret
labels for multi-year cohorts.
Choices are \code{"include"} (the default) and
\code{"exclude"}. See below for details.}

\item{interpret_fail}{Action if element of \code{labels}
cannot be interpreted. Choices are \code{"error"} (the default),
\code{"warn"}, and \code{"silent"}.}
}
\value{
\itemize{
\item \code{cohort_check()} returns a list with a logical flag
called \code{ok} and a \link[tibble:tibble]{tibble} called \code{details}.
\item \code{cohort_assert()} returns \code{labels} invisibly,
or raises an error.
}
}
\description{
\code{cohort_check()} reports on whether cohorts labels
meet conditions such as not overlapping.

\code{cohort_assert()} throws an error if conditions are not met.
}
\section{Controlling how cohort labels are interpreted}{


If \code{interpret_single} is \code{"lower"} (the default),
then labels for single-year cohorts
are assumed to refer to lower limits,
so that \code{"2025"} means \verb{[2025,2026)}.
This is the convention that data providers
typically use for calendar years.

If \code{interpret_single} is \code{"upper"},
then labels for single-year cohorts
are assumed to refer to upper limits,
so that \code{"2025"} means \verb{[2024,2025)}.
This is the convention that data providers
typically use for non-calendar years,
such as 1 July to 30 June.

If \code{interpret_multi} is \code{"include"} (the default),
then labels for multi-year cohorts
are assumed to include upper limits,
so that \code{"2025-2030"} means \verb{[2025,2030)}.

If \code{interpret_multi} is \code{"exclude"},
then labels for multi-year cohorts
are assumed to exclude the upper limits
so that \code{"2025-2030"} means \verb{[2025,2031)}.
}

\examples{
lab <- cohort_labels_five(
  lower_first = 2020,
  lower_last = 2030
)
lab

## get info on everything
cohort_check(
  labels = lab,
  no_overlap = TRUE,
  no_gap = TRUE,
  no_total = TRUE,
  no_na = TRUE,
  has_open_left = TRUE,
  has_open_right = TRUE
)

## throw error if overlap or gap
cohort_assert(
  labels = lab,
  no_overlap = TRUE,
  no_gap = TRUE
)
}
\seealso{
\itemize{
\item \code{\link[=age_check]{age_check()}} Age equivalent of \code{cohort_check()}
\item \code{\link[=period_check]{period_check()}} Period equivalent of \code{cohort_check()}
}
}
