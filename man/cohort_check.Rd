% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cohort_check.R
\name{cohort_check}
\alias{cohort_check}
\alias{cohort_assert}
\title{Check or Make Assertions About Cohorts}
\usage{
cohort_check(
  labels,
  no_overlap = NA,
  no_gap = NA,
  no_total = NA,
  no_na = NA,
  include_open = NA,
  interpret_single = c("lower", "upper"),
  interpret_multi = c("include", "exclude"),
  interpret_fail = c("error", "warn", "silent")
)

cohort_assert(
  labels,
  no_overlap = NA,
  no_gap = NA,
  no_total = NA,
  no_na = NA,
  include_open = NA,
  interpret_single = c("lower", "upper"),
  interpret_multi = c("include", "exclude"),
  interpret_fail = c("error", "warn", "silent")
)
}
\arguments{
\item{labels}{Vector of cohort labels.}

\item{no_overlap}{No cohorts overlap.}

\item{no_gap}{The cohorts span the entire
range from the lower limit of the earliest cohort
to the upper limit of the latest cohort.}

\item{no_total}{No "Total" label.}

\item{no_na}{No NA label.}

\item{include_open}{One or more cohorts
has no lower limit.}

\item{interpret_single}{How to interpret
labels for single-year cohorts.
Choices are \code{"lower"} (the default) and \code{"upper"}.
See below for details.}

\item{interpret_multi}{How to interpret
labels for multi-year cohorts.
Choices are \code{"include"} (the default) and
\code{"exclude"}. See below for details.}

\item{interpret_fail}{Action if element of \code{labels}
cannot be interpreted. Choices are \code{"error"} (the default),
\code{"warn"}, and \code{"silent"}.}
}
\value{
For \code{cohort_check()}, a list with logical \code{ok} and data frame
\code{details}; for \code{cohort_assert()}, \code{labels} invisibly or an error.
}
\description{
Collect information on cohort labels (\code{cohort_check()}),
or throw an error if cohort labels do not
conform to expectations (\code{cohort_assert}).
}
\details{
\tabular{llll}{
   Type \tab Interval \tab \code{format} argument \tab Label \cr
   single \tab \verb{[a, a+1)} \tab \code{format_single = "lower"} \tab \code{"year(a)"} \cr
   single \tab \verb{[a, a+1)} \tab \code{format_single = "upper"} \tab \code{"year(a+1)"} \cr
   range \tab \verb{[a, b)} \tab `format_multi = "include" \tab \code{"year(b)"} \cr
   range \tab \verb{[a, b)} \tab `format_multi = "exclude" \tab `"year(b)-1" \cr
   total \tab `(-Inf, Inf) \tab \if{html}{\out{<none>}} \tab \code{"Total"} \cr
   na \tab \verb{(NA, NA)} \tab \if{html}{\out{<none>}} \tab \code{NA} \cr
}
}
\examples{
lab <- cohort_labels_five(
  lower_first = 2020,
  lower_last = 2030
)
lab

## get info on everything
cohort_check(
  labels = lab,
  no_overlap = TRUE,
  no_gap = TRUE,
  no_total = TRUE,
  no_na = TRUE,
  include_open = TRUE
)

## throw error if gaps
cohort_assert(labels = lab, no_gap = TRUE)

lab_gap <- lab[c(1, 3)]
## throw error if no gaps
cohort_assert(lab_gap, no_gap = FALSE)
}
\seealso{
\itemize{
\item \code{\link[=age_check]{age_check()}} Age equivalent of \code{cohort_check()}
\item \code{\link[=period_check]{period_check()}} Period equivalent of \code{cohort_check()}
}
}
