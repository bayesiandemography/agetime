% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cohort_check.R
\name{cohort_check}
\alias{cohort_check}
\alias{cohort_assert}
\title{Check or Make Assertions About Cohorts}
\usage{
cohort_check(
  x,
  no_overlap = NA,
  no_gap = NA,
  no_total = NA,
  no_na = NA,
  include_open = NA,
  label_one = c("lower", "upper"),
  label_multi = c("include", "exclude"),
  unknown_label = c("error", "warn", "silent")
)

cohort_assert(
  x,
  no_overlap = NA,
  no_gap = NA,
  no_total = NA,
  no_na = NA,
  include_open = NA,
  label_one = c("lower", "upper"),
  label_multi = c("include", "exclude"),
  unknown_label = c("error", "warn", "silent")
)
}
\arguments{
\item{x}{A vector of cohort labels.}

\item{no_overlap}{No cohorts overlap}

\item{no_gap}{The cohorts span the entire
range from the lower limit of the earliest cohort
to the upper limit of the latest cohort}

\item{no_total}{No "Total" label}

\item{no_na}{No NA label}

\item{include_open}{One or more cohorts
has no lower limit.}

\item{label_one}{Whether labels
for one-year cohorts are based on the
lower or upper limit of the period.
Default is \code{"lower"}.}

\item{label_multi}{Whether
labels for multi-year periods
include or exclude the final
year of the period.
Default is \code{"include"}.}

\item{unknown_label}{Action if a label
cannot be interpreted. Choices are
\code{"error"} (the default), \code{"warn"},
and \code{"silent"}.}
}
\value{
\itemize{
\item \code{cohort_check()} returns a list with
components \code{ok} (a logical flag)
and \code{details} (a data frame).
\item \code{cohort_assert()} returns \code{x} invisibly,
or throws an error.
}
}
\description{
Collect information on cohort labels (\code{cohort_check()}),
or throw an error if cohort labels do not
conform to expectations (\code{cohort_assert}).
}
\examples{
lab <- cohort_labels_five(lower_first = 2020,
                          lower_last = 2030)
lab

## get info on everything
cohort_check(x = lab,
             no_overlap = TRUE,
             no_gap = TRUE,
             no_total = TRUE,
             no_na = TRUE,
             include_open = TRUE)

## throw error if gaps
cohort_assert(x = lab, no_gap = TRUE)

lab_gap <- lab[c(1, 3)]
## throw error if no gaps
cohort_assert(lab_gap, no_gap = FALSE)
}
