% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/age_levels_fill.R
\name{age_levels_fill}
\alias{age_levels_fill}
\alias{age_levels_fill_one}
\alias{age_levels_fill_five}
\alias{age_levels_fill_ten}
\alias{age_levels_fill_life}
\title{Fill in Gaps in Age Group Levels}
\usage{
age_levels_fill(
  labels,
  breaks = NULL,
  interpret_fail = c("error", "warn", "silent")
)

age_levels_fill_one(labels, interpret_fail = c("error", "warn", "silent"))

age_levels_fill_five(labels, interpret_fail = c("error", "warn", "silent"))

age_levels_fill_ten(labels, interpret_fail = c("error", "warn", "silent"))

age_levels_fill_life(labels, interpret_fail = c("error", "warn", "silent"))
}
\arguments{
\item{labels}{Vector of age group labels.}

\item{breaks}{Boundaries of newly-created age groups.
Boundaries for existing age groups can be omitted.}

\item{interpret_fail}{Action if element of \code{labels}
cannot be interpreted. Choices are \code{"error"} (the default),
\code{"warn"}, and \code{"silent"}.}
}
\value{
Factor with the same length as \code{labels}.
}
\description{
Fill in gaps in levels of \code{labels}.
}
\details{
If \code{labels} is not a factor, and so does
not have levels, convert it to
a factor before filling in levels.
\itemize{
\item \code{age_levels_fill} adds age groups
specified by \code{breaks}.
\item \code{age_levels_fill_one} adds age groups with
width 1.
\item \code{age_levels_fill_five} adds age groups with
width 5.
\item \code{age_levels_fill_ten} adds age groups with
width 10.
\item \code{age_levels_fill_life} adds age groups
used by an abridged life table.
}
}
\section{Abridged and complete life tables}{

\itemize{
\item An 'abridged' life table uses age groups \code{"0"}
and \code{"1-4"}, followed by 5-year age groups \code{"5-9"}, \code{"10-14"}, ...
\item A 'complete' life table uses single-year age groups
\code{"0"}, \code{"1"}, \code{"2"}, ...
\item Both types of life table have an open interval
such as \code{"85+"} or \code{"100+"}.
}
}

\examples{
labels <- factor(c("0-4", "20-24"))
labels
age_levels_fill(labels) ## uses existing boundaries
age_levels_fill(labels, breaks = c(8, 12))
age_levels_fill_one(labels)
age_levels_fill_five(labels)

labels <- c("25-29", "0-4")
age_levels_fill_ten(labels)

labels <- c("60+", "0")
age_levels_fill_life(labels)

## levels are used by functions
## such as 'table()'
labels <- c("30-39", "0-9")
labels |> table()
labels |>
  age_levels_fill() |>
  table()

## sort after filling
labels |>
  age_levels_fill() |>
  age_levels_sort() |>
  table()
}
\seealso{
\itemize{
\item \code{\link[=period_levels_fill]{period_levels_fill()}} Period equivalent of \code{age_levels_fill()}
\item \code{\link[=cohort_levels_fill]{cohort_levels_fill()}} Cohort equivalent of \code{age_levels_fill()}
}
}
