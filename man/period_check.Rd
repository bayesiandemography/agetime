% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/period_check.R
\name{period_check}
\alias{period_check}
\alias{period_assert}
\title{Check or Make Assertions About Periods}
\usage{
period_check(
  labels,
  no_overlap = FALSE,
  no_gap = FALSE,
  no_total = FALSE,
  no_na = FALSE,
  has_open_left = FALSE,
  has_open_right = FALSE,
  interpret_single = c("lower", "upper"),
  interpret_multi = c("include", "exclude"),
  interpret_fail = c("error", "warn", "silent")
)

period_assert(
  labels,
  no_overlap = FALSE,
  no_gap = FALSE,
  no_total = FALSE,
  no_na = FALSE,
  has_open_left = FALSE,
  has_open_right = FALSE,
  interpret_single = c("lower", "upper"),
  interpret_multi = c("include", "exclude"),
  interpret_fail = c("error", "warn", "silent")
)
}
\arguments{
\item{labels}{Vector of period labels.}

\item{no_overlap}{Check that no periods overlap.
Default is \code{FALSE} (don't check).}

\item{no_gap}{Check that all periods between
the earliest and latest periods are included.
Default is \code{FALSE} (don't check).}

\item{no_total}{Check that there is no "Total" label.
Default is \code{FALSE} (don't check).}

\item{no_na}{Check that there is no \code{NA} label.
Default is \code{FALSE} (don't check).}

\item{has_open_left}{Check that at least one period
is open on the left.
Default is \code{FALSE} (don't check).}

\item{has_open_right}{Check that at least one period
is open on the right.
Default is \code{FALSE} (don't check).}

\item{interpret_single}{How to interpret
labels for single-year periods.
Choices are \code{"lower"} (the default) and \code{"upper"}.
See below for details.}

\item{interpret_multi}{How to interpret
labels for multi-year periods.
Choices are \code{"include"} (the default) and
\code{"exclude"}. See below for details.}

\item{interpret_fail}{Action if element of \code{labels}
cannot be interpreted. Choices are \code{"error"} (the default),
\code{"warn"}, and \code{"silent"}.}
}
\value{
\itemize{
\item \code{period_check()} returns a list with a logical flag
called \code{ok} and a \link[tibble:tibble]{tibble} called \code{details}.
\item \code{period_assert()} returns \code{labels} invisibly,
or raises an error.
}
}
\description{
\code{period_check()} reports on whether period labels
meet conditions such as not overlapping.

\code{period_assert()} throws an error if conditions are not met.
}
\section{Controlling how period labels are interpreted}{


If \code{interpret_single} is \code{"lower"} (the default),
then labels for single-year periods
are assumed to refer to lower limits,
so that \code{"2025"} means \verb{[2025,2026)}.
This is the convention that data
providers typically use
for calendar years.

If \code{interpret_single} is \code{"upper"},
then labels for single-year periods
are assumed to refer to upper limits,
so that \code{"2025"} means \verb{[2024,2025)}.
This is the convention that data providers
typically use for non-calendar years,
such as 1 July to 30 June.

If \code{interpret_multi} is \code{"include"} (the default),
then labels for multi-year periods
are assumed to include upper limits,
so that \code{"2025-2030"} means \verb{[2025,2030)}.

If \code{interpret_multi} is \code{"exclude"},
then labels for multi-year periods
are assumed to exclude the upper limits,
so that \code{"2025-2030"} means \verb{[2025,2031)}.
}

\examples{
lab <- period_labels_five(
  lower_first = 2020,
  lower_last = 2030
)
lab

## get info on everything
period_check(
  labels = lab,
  no_overlap = TRUE,
  no_gap = TRUE,
  no_total = TRUE,
  no_na = TRUE
)

## throw error if overlap or gap
period_assert(
  labels = lab,
  no_overlap = TRUE,
  no_gap = TRUE
)
}
\seealso{
\itemize{
\item \code{\link[=age_check]{age_check()}} Age equivalent of \code{period_check()}
\item \code{\link[=cohort_check]{cohort_check()}} Cohort equivalent of \code{period_check()}
}
}
