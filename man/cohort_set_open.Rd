% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cohort_set_open.R
\name{cohort_set_open}
\alias{cohort_set_open}
\title{Define Open Cohort}
\usage{
cohort_set_open(
  x,
  open,
  x_one = c("lower", "upper"),
  x_multi = c("include", "exclude"),
  x_fail = c("error", "warn", "silent")
)
}
\arguments{
\item{x}{A vector of cohort labels.}

\item{open}{Upper limit of open cohort.}

\item{x_one}{Whether labels
for one-year cohorts are based on the
lower or upper limit of the period.
Default is \code{"lower"}.}

\item{x_multi}{Whether
labels for multi-year periods
include or exclude the final
year of the period.
Default is \code{"include"}.}

\item{x_fail}{Action if element of \code{x}
cannot be parsed: \code{"error"} (the default),
\code{"warn"}, or \code{"silent"}.}
}
\value{
A factor if \code{x} is a factor; otherwise a character vector.
}
\description{
Set an open cohort, i.e. a cohort with no
lower limit. Replace existing
cohorts where necessary.
}
\examples{
x <- c("2020-2024", "<2000", "2015")
cohort_set_open(x, open = 2020)
cohort_set_open(x, open = 2005)
}
\seealso{
\code{\link[=age_set_open]{age_set_open()}} Age equivalent of \code{cohort_set_open()}
}
