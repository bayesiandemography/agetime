% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/period_standard.R
\name{period_standard}
\alias{period_standard}
\title{Standardize Period Labels}
\usage{
period_standard(
  x,
  x_one = c("lower", "upper"),
  x_multi = c("include", "exclude"),
  x_fail = c("error", "warn", "silent")
)
}
\arguments{
\item{x}{Vector of period labels.}

\item{x_one}{How to interpret
labels in \code{x} that describe one-year periods.
Choices are \code{"lower"} (the default) and \code{"upper"}.}

\item{x_multi}{How to interpret
labels in \code{x} that describe multi-year periods.
Choices are \code{"include"} (the default) and
\code{"exclude"}.}

\item{x_fail}{Action if element of \code{x}
cannot be parsed: \code{"error"} (the default),
\code{"warn"}, or \code{"silent"}.}
}
\value{
A vector the same length as \code{x} with standardized labels.

If \code{x} is a character vector, returns a character vector.
If \code{x} is a factor, returns a factor with the same length and
\code{ordered} attribute as \code{x}. Element values and \code{levels(x)} are
standardized; level order is unchanged except where two or more
levels collapse to the same string after standardization (the first
occurrence in \code{levels(x)} is kept).
When \code{length(x) == 0}, standardized \code{levels(x)} are still applied.

With \code{x_fail = "silent"}, labels that cannot be parsed are mapped to
\code{NA} (including in \code{levels()} for factors).
}
\description{
Convert period labels to a 'standard' format.
}
\examples{
x <- c("2025to2030", "1910--1914", " 2022 ")
period_standard(x)
}
