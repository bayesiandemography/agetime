% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/period_convert.R
\name{period_convert_five}
\alias{period_convert_five}
\alias{period_convert_ten}
\title{Convert to Equal-Length Periods}
\usage{
period_convert_five(
  x,
  offset = 0,
  x_one = c("lower", "upper"),
  x_multi = c("include", "exclude"),
  x_fail = c("error", "warn", "silent")
)

period_convert_ten(
  x,
  offset = 0,
  x_one = c("lower", "upper"),
  x_multi = c("include", "exclude"),
  x_fail = c("error", "warn", "silent")
)
}
\arguments{
\item{x}{Vector of period labels.}

\item{offset}{Parameter controlling
alignment of periods. Defult is \code{0}.}

\item{x_one}{How to interpret
labels in \code{x} that describe one-year periods.
Choices are \code{"lower"} (the default) and \code{"upper"}.}

\item{x_multi}{How to interpret
labels in \code{x} that describe multi-year periods.
Choices are \code{"include"} (the default) and
\code{"exclude"}.}

\item{x_fail}{What to do if a
label in \code{x} cannot be interpreted.
Choices are
\code{"error"} (the default), \code{"warn"},
and \code{"silent"}.}
}
\value{
If \code{x} is a factor, then the return value is
a factor; otherwise it is a character vector.
}
\description{
Modify the periods used by \code{x}.
The new periods must contain
the old periods, and follow a regular
pattern:
\itemize{
\item \code{period_convert_five} Five-year periods
\item \code{period_convert_ten} Ten-year periods
}
}
\examples{
x <- c("2002-2004", "1987-1989", "2000", "Total")
period_convert_five(x)
period_covert_five(x, offset = 1)
period_covert_five(x, offset = 2)
period_covert_ten(x)
period_covert_ten(x, offset = 1)
period_covert_ten(x, offset = 2)
}
\seealso{
\code{\link[=period_convert]{period_convert()}} Convert to general periods
\code{\link[=age_convert_five]{age_convert_five()}} Age equivalent of \code{period_convert_five()}
\code{\link[=age_convert_ten]{age_convert_ten()}} Age equivalent of \code{period_convert_ten()}
\code{\link[=cohort_convert_five]{cohort_convert_five()}} Cohort equivalent of \code{period_convert_five()}
\code{\link[=cohort_convert_ten]{cohort_convert_ten()}} Cohort equivalent of \code{period_convert_ten()}
period_fill()] Add levels for intermediate periods
}
