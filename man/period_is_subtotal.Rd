% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/period_is_subtotal.R
\name{period_is_subtotal}
\alias{period_is_subtotal}
\title{Identify Period Labels for Subtotals}
\usage{
period_is_subtotal(
  labels,
  interpret_single = c("lower", "upper"),
  interpret_multi = c("include", "exclude"),
  interpret_fail = c("error", "warn", "silent")
)
}
\arguments{
\item{labels}{Vector of period labels.}

\item{interpret_single}{How to interpret
labels for single-year periods.
Choices are \code{"lower"} (the default) and \code{"upper"}.
See below for details.}

\item{interpret_multi}{How to interpret
labels for multi-year periods.
Choices are \code{"include"} (the default) and
\code{"exclude"}. See below for details.}

\item{interpret_fail}{Action if element of \code{labels}
cannot be interpreted. Choices are \code{"error"} (the default),
\code{"warn"}, and \code{"silent"}.}
}
\value{
Logical vector with the same length as \code{labels}.
}
\description{
Find categories representing subtotals in period labels.
}
\details{
A subtotal is a period that is fully covered by
other periods in the same set of labels.
For instance, in the labels
\code{c("2020-2025", "2025-2030", "2030-2035", "2020-2030", "Total")},
\code{"2020-2030"} is a subtotal.

Subtotals are distinct from grand total labels
such as \code{"Total"} or \code{"All"}, which are identified
by \code{\link[=period_is_total]{period_is_total()}}.
}
\section{Rules for interpreting inputs}{


Single-year periods:\tabular{lll}{
   \code{interpret_single} \tab \emph{Rule} \tab \emph{Example} \cr
   \code{"lower"} \tab \verb{"a" -> [a,a+1)} \tab \verb{"2020" -> [2020,2021)} \cr
   \code{"upper"} \tab \verb{"a" -> [a-1,a)} \tab \verb{"2020" -> [2019,2020)} \cr
}


Data providers typically use the "lower" convention
for calendar years (1 January to 31 December),
and the "upper" convention for non-calendar years
(e.g., 1 July to 30 June).

Multi-year periods:\tabular{lll}{
   \code{interpret_multi} \tab \emph{Rule} \tab \emph{Example} \cr
   \code{"include"} \tab \verb{"a-<a+n>" -> [a,a+n)} \tab \verb{"2020-2025" -> [2020,2025)} \cr
   \code{"exclude"} \tab \verb{"a-<a+n-1>" -> [a,a+n)} \tab \verb{"2020-2024" -> [2020,2025)} \cr
}
}

\examples{
labels <- c(
  period_labels_five(lower_first = 2000, lower_last = 2035),
  "2000-2015",
  "2015-2040",
  "Total"
)
period_is_subtotal(labels)
period_is_total(labels)

## subtotals must fully cover range
labels_no_2020_2025 <- setdiff(labels, "2020-2025")
period_is_subtotal(labels_no_2020_2025) ## "2015-2040" not subtotal

## subtotals can overlap
labels_2000_2040 <- c(labels, "2000-2040")
period_is_subtotal(labels_2000_2040)

## use to filter data
library(dplyr, warn.conflicts = FALSE)
df <- data.frame(
  period = c("2020-2025", "2025-2030", "2030-2035", "2020-2035"),
  value = c(100, 200, 300, 400)
)
df |>
  filter(!period_is_subtotal(period))
}
\seealso{
\itemize{
\item \code{\link[=age_is_subtotal]{age_is_subtotal()}} Age equivalent of \code{period_is_subtotal()}
\item \code{\link[=cohort_is_subtotal]{cohort_is_subtotal()}} Cohort equivalent of \code{period_is_subtotal()}
\item \code{\link[=period_is_total]{period_is_total()}} Identify grand totals
\item \code{\link[=period_is_missing]{period_is_missing()}} Identify missing period labels
\item \code{\link[=period_is_open_left]{period_is_open_left()}} Identify periods open on left
\item \code{\link[=period_is_open_right]{period_is_open_right()}} Identify periods open on right
}
}
