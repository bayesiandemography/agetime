% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/age_convert.R
\name{age_convert}
\alias{age_convert}
\title{Convert to New Age Groups}
\usage{
age_convert(x, breaks, open = TRUE, x_fail = c("error", "warn", "silent"))
}
\arguments{
\item{x}{Vector of age group labels.}

\item{breaks}{Boundaries between age groups.
A numeric vector.}

\item{open}{Whether the oldest age group
is "open", i.e. has no upper limit.
Default is \code{TRUE}.}

\item{x_fail}{Action if meaning of label
unclear. Choices are \code{"error"} (the default),
\code{"warn"}, and \code{"silent"}.}
}
\value{
If \code{x} is a factor, then the return value is
a factor; otherwise it is a character vector.
}
\description{
Modify the age groups used by \code{x}. The
the new age groups must
contain the old ones.
}
\examples{
x <- c("1-4", "87-89", "0", "50-54")
age_convert(x, breaks = c(0, 10, 40, 90))
age_convert(x, breaks = c(0, 10, 40, 90), open = FALSE)
}
\seealso{
\code{\link[=age_convert_five]{age_convert_five()}} Convert to 5-year age groups
\code{\link[=age_convert_ten]{age_convert_ten()}} Convert to 10-year age groups
\code{\link[=age_convert_life]{age_convert_life()}} Convert to life table age groups
\code{\link[=period_convert]{period_convert()}} Period equivalent of \code{age_convert()}
\code{\link[=cohort_convert]{cohort_convert()}} Cohort equivalent of \code{age_convert()}
}
