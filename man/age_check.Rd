% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/age_check.R
\name{age_check}
\alias{age_check}
\alias{age_assert}
\title{Check or Make Assertions About Age Groups}
\usage{
age_check(
  x,
  no_overlap = NA,
  no_gap = NA,
  no_total = NA,
  no_na = NA,
  include_zero = NA,
  include_open = NA,
  valid_life = NA,
  x_fail = c("error", "warn", "silent")
)

age_assert(
  x,
  no_overlap = NA,
  no_gap = NA,
  no_total = NA,
  no_na = NA,
  include_zero = NA,
  include_open = NA,
  valid_life = NA,
  x_fail = c("error", "warn", "silent")
)
}
\arguments{
\item{x}{Vector of age group labels.}

\item{no_overlap}{No age groups overlap.}

\item{no_gap}{Age groups span entire
range from lower limit of youngest age
group to upper limit of oldest age group.}

\item{no_total}{No "Total" age group.}

\item{no_na}{No NA age group.}

\item{include_zero}{One or more age groups
have a lower limit of zero.}

\item{include_open}{One or more age groups
has no upper limit.}

\item{valid_life}{All labels valid for
(abridged) life table.}

\item{x_fail}{Action if element of \code{x}
cannot be parsed: \code{"error"} (the default),
\code{"warn"}, or \code{"silent"}.}
}
\value{
For \code{age_check()}, a list with logical \code{ok} and data frame
\code{details}; for \code{age_assert()}, \code{x} invisibly or an error.
}
\description{
Collect information on age group labels (\code{age_check()}),
or throw an error if age group labels do not
conform to expectations (\code{age_assert}).
}
\examples{
lab <- age_labels_life()
lab

## get info on everything
age_check(
  x = lab,
  no_overlap = TRUE,
  no_gap = TRUE,
  no_total = TRUE,
  no_na = TRUE,
  include_zero = TRUE,
  include_open = TRUE,
  valid_life = TRUE
)

## throw error if gaps
age_assert(x = lab, no_gap = TRUE)

lab_gap <- lab[c(1, 3)]
## throw error if no gaps
age_assert(x = lab_gap, no_gap = FALSE)
}
\seealso{
\itemize{
\item \code{\link[=period_check]{period_check()}} Period equivalent of \code{age_check()}
\item \code{\link[=cohort_check]{cohort_check()}} Cohort equivalent of \code{age_check()}
}
}
