% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/age_check.R
\name{age_check}
\alias{age_check}
\alias{age_assert}
\title{Check or Make Assertions About Age Groups}
\usage{
age_check(
  x,
  no_overlap = NA,
  no_gap = NA,
  no_total = NA,
  no_na = NA,
  include_zero = NA,
  include_open = NA,
  unknown_label = c("error", "warn", "silent")
)

age_assert(
  x,
  no_overlap = NA,
  no_gap = NA,
  no_total = NA,
  no_na = NA,
  include_zero = NA,
  include_open = NA,
  unknown_label = c("error", "warn", "silent")
)
}
\arguments{
\item{x}{Vector of age group labels.}

\item{no_overlap}{No age groups overlap}

\item{no_gap}{The age groups span the entire
range from the lower limit of the youngest age
group to the upper limit of the oldest age group}

\item{no_total}{No "Total" age group}

\item{no_na}{No NA age group}

\item{include_zero}{One or more age groups
have a lower limit of zero.}

\item{include_open}{One or more age groups
has no upper limit.}

\item{unknown_label}{Action if meaning of label
unclear. Choices are \code{"error"} (the default),
\code{"warn"}, and \code{"silent"}.}
}
\value{
\itemize{
\item \code{age_check()} returns a list with
components \code{ok} (a logical flag)
and \code{details} (a data frame).
\item \code{age_assert()} returns \code{x} invisibly,
or throws an error.
}
}
\description{
Collect information on age group labels (\code{age_check()}),
or throw an error if age group labels do not
conform to expectations (\code{age_assert}).
}
\examples{
lab <- age_labels_labor()
lab

## get info on everything
age_check(x = lab,
          no_overlap = TRUE,
          no_gap = TRUE,
          no_total = TRUE,
          no_na = TRUE,
          include_zero = TRUE,
          include_open = TRUE)

## throw error if gaps
age_assert(x = lab, no_gap = TRUE)

lab_gap <- lab[c(1, 3)]
## throw error if no gaps
age_assert(x = lab_gap, no_gap = FALSE)
}
