% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/age_check.R
\name{age_check}
\alias{age_check}
\alias{age_assert}
\title{Check or Make Assertions About Age Groups}
\usage{
age_check(
  labels,
  no_overlap = FALSE,
  no_gap = FALSE,
  no_total = FALSE,
  no_na = FALSE,
  has_zero = FALSE,
  has_open_right = FALSE,
  valid_life = FALSE,
  interpret_fail = c("error", "warn", "silent")
)

age_assert(
  labels,
  no_overlap = FALSE,
  no_gap = FALSE,
  no_total = FALSE,
  no_na = FALSE,
  has_zero = FALSE,
  has_open_right = FALSE,
  valid_life = FALSE,
  interpret_fail = c("error", "warn", "silent")
)
}
\arguments{
\item{labels}{Vector of age group labels.}

\item{no_overlap}{Check that no age groups overlap.
Default is \code{FALSE} (don't check).}

\item{no_gap}{Check that all ages between the
youngest and oldest age groups are included.
Default is \code{FALSE} (don't check).}

\item{no_total}{Check that there is no total category.
Default is \code{FALSE} (don't check).}

\item{no_na}{Check that there is no \code{NA} category.
Default is \code{FALSE} (don't check).}

\item{has_zero}{Check that at least one
age group has a lower limit of zero.
Default is \code{FALSE} (don't check).}

\item{has_open_right}{Check that at least one age
group is open on the right (has no upper limit).
Default is \code{FALSE} (don't check).}

\item{valid_life}{Check that all labels are
valid for an abridged life table.
Default is \code{FALSE} (don't check).}

\item{interpret_fail}{Action if element of \code{labels}
cannot be interpreted. Choices are \code{"error"} (the default),
\code{"warn"}, and \code{"silent"}.}
}
\value{
\itemize{
\item \code{age_check()} returns a list with a logical flag
called \code{ok} and a \link[tibble:tibble]{tibble} called \code{details}.
\item \code{age_assert()} returns \code{labels} invisibly,
or raises an error.
}
}
\description{
\code{age_check()} reports whether age group labels
meet conditions.

\code{age_assert()} throws an error if conditions are not met.

Conditions include:
\itemize{
\item Not overlapping
\item Not having gaps
\item Having totals, NAs, or zeros
\item Having open age groups
\item Valid for an abridged life table
}
}
\section{Abridged and complete life tables}{

\itemize{
\item An abridged life table uses age groups \code{"0"}
and \code{"1-4"}, followed by 5-year age groups \code{"5-9"}, \code{"10-14"}, ...
\item A complete life table uses single-year age groups
\code{"0"}, \code{"1"}, \code{"2"}, ...
\item Both types of life table usually have an open age group
such as \code{"85+"} or \code{"100+"}.
}
}

\examples{
lab <- age_labels_life()
lab

## get info on everything
age_check(
  labels = lab,
  no_overlap = TRUE,
  no_gap = TRUE,
  no_total = TRUE,
  no_na = TRUE,
  has_zero = TRUE,
  has_open_right = TRUE,
  valid_life = TRUE
)

## throw error if overlap or gap
age_assert(
  labels = lab,
  no_overlap = TRUE,
  no_gap = TRUE
)
}
\seealso{
\itemize{
\item \code{\link[=period_check]{period_check()}} Period equivalent of \code{age_check()}
\item \code{\link[=cohort_check]{cohort_check()}} Cohort equivalent of \code{age_check()}
}
}
