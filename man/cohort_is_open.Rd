% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cohort_is_total.R
\name{cohort_is_open}
\alias{cohort_is_open}
\title{Identify Cohort Labels for Open Cohorts}
\usage{
cohort_is_open(
  x,
  x_one = c("lower", "upper"),
  x_multi = c("include", "exclude"),
  x_fail = c("error", "warn", "silent")
)
}
\arguments{
\item{x}{Vector of cohort labels.}

\item{x_one}{Whether labels
for one-year cohorts are based on
lower or upper limit of period.
Default is \code{"lower"}.}

\item{x_multi}{Whether
labels for multi-year periods
include or exclude final
year of period.
Default is \code{"include"}.}

\item{x_fail}{Action if element of \code{x}
cannot be parsed: \code{"error"} (the default),
\code{"warn"}, or \code{"silent"}.}
}
\value{
Logical vector with the same length as \code{x}.
}
\description{
Find cohort labels that \pkg{agetime} interprets as
open, ie  having no lower limit.
}
\examples{
x <- c("2020", "<1900", "2040-2050", "<2022")
cohort_is_open(x)
}
\seealso{
\itemize{
\item \code{\link[=parsing_cohort_labels]{parsing_cohort_labels()}} Details for \code{x_one}, \code{x_multi}, and \code{x_fail}
\item \code{\link[=cohort_is_total]{cohort_is_total()}} Find cohort labels for totals
\item \code{\link[=age_is_open]{age_is_open()}} Age equivalent of \code{cohort_is_open()}
}
}
