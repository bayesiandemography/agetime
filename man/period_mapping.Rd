% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/period_mapping.R
\name{period_mapping}
\alias{period_mapping}
\title{Mapping Between Period Labels}
\usage{
period_mapping(
  x,
  y = NULL,
  relation = c("equals", "contains", "is-contained-in", "overlaps-with"),
  format = c("tibble", "matrix"),
  x_one = c("lower", "upper"),
  x_multi = c("include", "exclude"),
  x_fail = c("error", "warn", "silent")
)
}
\arguments{
\item{x}{Vector of period labels.}

\item{y}{Vector of period labels. If
no value supplied, \code{x} is mapped onto itself.}

\item{relation}{Relationship between
labels. Choices are \code{"equals"} (the default),
\code{"contains"}, \code{"is-contained-in"}, and \code{"overlaps-with"}.
See below for details and examples.}

\item{format}{Format of
return value. Choices are \code{"tibble"}
(the default) or \code{"matrix"}.}

\item{x_one}{How to interpret
labels in \code{x} that describe one-year periods.
Choices are \code{"lower"} (the default) and \code{"upper"}.}

\item{x_multi}{How to interpret
labels in \code{x} that describe multi-year periods.
Choices are \code{"include"} (the default) and
\code{"exclude"}.}

\item{x_fail}{Action if element of \code{x}
cannot be parsed: \code{"error"} (the default),
\code{"warn"}, or \code{"silent"}.}
}
\value{
\link[tibble:tibble]{Tibble} or matrix, depending on \code{format}.
}
\description{
Create a mapping between period labels. A mapping
depicts a relationship between the labels of \code{x}
and the labels of \code{y}. The types of relationship
that can be mapped are:
\itemize{
\item "x equals y"
\item "x contains y"
\item "x is contained in y"
\item "x overlaps with y".
}
}
\details{
If no value for \code{y} is supplied,
\code{x} is mapped onto itself.

Tibbles produced by \code{period_mapping()} are sparse
in that they only include matches. Matrices
produced by \code{period_mapping()} are dense in that
they include matches and non-matches. See the
example below.
}
\section{The \code{relation} argument}{
\tabular{ll}{
   \code{relation} \tab Endpoints of \code{x} and \code{y} \cr
   \code{"equals"} \tab Endpoints equal \cr
   \code{"contains"} \tab Endpoints of \code{y} inside endpoints of \code{x} \cr
   \code{"is-contained-in"} \tab Endpoints of \code{x} inside endpoints of \code{y} \cr
   \code{"overlaps-with"} \tab Endpoint of \code{x} in \code{y} or endpoint of \code{y} in \code{x} \cr
}
}

\examples{
x <- c("2020-2025", "2030", "2025-2027")
y <- c("2025-2030", "2020-2025", "2026-2034")
period_mapping(x = x, y = y)
period_mapping(x = x, y = y, format = "matrix")
period_mapping(x = x, y = y, relation = "contains")
period_mapping(x = x, y = y, relation = "is-contained-in")
period_mapping(x = x, y = y, relation = "overlaps-with")

# sparse tibble vs dense matrix
x <- c("2020-2025", "2030-2035")
y <- c("2020-2025", "2025-2030")
period_mapping(x = x, y = y) # one match
period_mapping(x = x, y = y, format = "matrix") # 1 match and 3 non-matches

# mapping 'x' on to itself
x <- c("2020--2025", "2020-2025", "2030")
period_mapping(x)
}
\seealso{
\itemize{
\item \code{\link[=parsing_period_labels]{parsing_period_labels()}} Details for \code{x_one}, \code{x_multi}, and \code{x_fail}
\item \code{\link[=age_mapping]{age_mapping()}} Age equivalent of \code{period_mapping()}
\item \code{\link[=cohort_mapping]{cohort_mapping()}} Cohort equivalent of \code{period_mapping()}
}
}
