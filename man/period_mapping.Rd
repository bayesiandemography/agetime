% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/period_mapping.R
\name{period_mapping}
\alias{period_mapping}
\title{Mapping Between Period Labels}
\usage{
period_mapping(
  labels_x,
  labels_y = NULL,
  relation = c("equals", "contains", "is-contained-in", "overlaps-with"),
  format = c("tibble", "matrix"),
  interpret_single = c("lower", "upper"),
  interpret_multi = c("include", "exclude"),
  interpret_fail = c("error", "warn", "silent")
)
}
\arguments{
\item{labels_x}{Vector of period labels.}

\item{labels_y}{Vector of period labels. If
no value supplied, \code{labels_x} is mapped onto itself.}

\item{relation}{Relationship between
labels. Choices are \code{"equals"} (the default),
\code{"contains"}, \code{"is-contained-in"}, and \code{"overlaps-with"}.
See below for details and examples.}

\item{format}{Format of
return value. Choices are \code{"tibble"}
(the default) or \code{"matrix"}.}

\item{interpret_single}{How to interpret
labels for single-year periods.
Choices are \code{"lower"} (the default) and \code{"upper"}.
See below for details.}

\item{interpret_multi}{How to interpret
labels for multi-year periods.
Choices are \code{"include"} (the default) and
\code{"exclude"}. See below for details.}

\item{interpret_fail}{Action if element of \code{labels}
cannot be interpreted. Choices are \code{"error"} (the default),
\code{"warn"}, and \code{"silent"}.}
}
\value{
\link[tibble:tibble]{Tibble} or matrix, depending on \code{format}.
}
\description{
Create a mapping between period labels. A mapping
depicts a relationship between the labels of \code{labels_x}
and the labels of \code{labels_y}. The types of relationship
that can be mapped are:
\itemize{
\item "labels_x equals labels_y"
\item "labels_x contains labels_y"
\item "labels_x is contained in labels_y"
\item "labels_x overlaps with labels_y".
}
}
\details{
If no value for \code{labels_y} is supplied,
\code{labels_x} is mapped onto itself.

Tibbles produced by \code{period_mapping()} are sparse
in that they only include matches. Matrices
produced by \code{period_mapping()} are dense in that
they include matches and non-matches. See the
example below.
}
\section{The \code{relation} argument}{
\tabular{ll}{
   \code{relation} \tab Endpoints of \code{labels_x} and \code{labels_y} \cr
   \code{"equals"} \tab Endpoints equal \cr
   \code{"contains"} \tab Endpoints of \code{labels_y} inside endpoints of \code{labels_x} \cr
   \code{"is-contained-in"} \tab Endpoints of \code{labels_x} inside endpoints of \code{labels_y} \cr
   \code{"overlaps-with"} \tab Endpoint of \code{labels_x} in \code{labels_y}, or reverse \cr
}
}

\section{Controlling how period labels are interpreted}{


If \code{interpret_single} is \code{"lower"} (the default),
then labels for single-year periods
are assumed to refer to lower limits,
so that \code{"2025"} means \verb{[2025,2026)}.
This is the convention that data
providers typically use
for calendar years.

If \code{interpret_single} is \code{"upper"},
then labels for single-year periods
are assumed to refer to upper limits,
so that \code{"2025"} means \verb{[2024,2025)}.
This is the convention that data providers
typically use for non-calendar years,
such as 1 July to 30 June.

If \code{interpret_multi} is \code{"include"} (the default),
then labels for multi-year periods
are assumed to include upper limits,
so that \code{"2025-2030"} means \verb{[2025,2030)}.

If \code{interpret_multi} is \code{"exclude"},
then labels for multi-year periods
are assumed to exclude the upper limits,
so that \code{"2025-2030"} means \verb{[2025,2031)}.
}

\examples{
labels_x <- c("2020-2025", "2030", "2025-2027")
labels_y <- c("2025-2030", "2020-2025", "2026-2034")
period_mapping(labels_x = labels_x, labels_y = labels_y)
period_mapping(labels_x = labels_x, labels_y = labels_y, format = "matrix")
period_mapping(labels_x = labels_x, labels_y = labels_y, relation = "contains")
period_mapping(labels_x = labels_x, labels_y = labels_y, relation = "is-contained-in")
period_mapping(labels_x = labels_x, labels_y = labels_y, relation = "overlaps-with")

# sparse tibble vs dense matrix
labels_x <- c("2020-2025", "2030-2035")
labels_y <- c("2020-2025", "2025-2030")
period_mapping(labels_x = labels_x, labels_y = labels_y) # one match
period_mapping(labels_x = labels_x, labels_y = labels_y, format = "matrix")

# mapping 'labels_x' on to itself
labels_x <- c("2020--2025", "2020-2025", "2030")
period_mapping(labels_x)
}
\seealso{
\itemize{
\item \code{\link[=age_mapping]{age_mapping()}} Age equivalent of \code{period_mapping()}
\item \code{\link[=cohort_mapping]{cohort_mapping()}} Cohort equivalent of \code{period_mapping()}
}
}
