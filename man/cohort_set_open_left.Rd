% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cohort_set_open_left.R,
%   R/cohort_set_open_right.R
\name{cohort_set_open_left}
\alias{cohort_set_open_left}
\alias{cohort_set_open_right}
\title{Specify Open Cohort}
\usage{
cohort_set_open_left(
  labels,
  at,
  interpret_single = c("lower", "upper"),
  interpret_multi = c("include", "exclude"),
  interpret_fail = c("error", "warn", "silent")
)

cohort_set_open_right(
  labels,
  at,
  interpret_single = c("lower", "upper"),
  interpret_multi = c("include", "exclude"),
  interpret_fail = c("error", "warn", "silent")
)
}
\arguments{
\item{labels}{Vector of cohort labels.}

\item{at}{Point at which intervals become open,
e.g. \code{2020} in \verb{<2020} or \code{2030} in \verb{2030+}.}

\item{interpret_single}{How to interpret
labels for single-year cohorts.
Choices are \code{"lower"} (the default) and \code{"upper"}.
See below for details.}

\item{interpret_multi}{How to interpret
labels for multi-year cohorts.
Choices are \code{"include"} (the default) and
\code{"exclude"}. See below for details.}

\item{interpret_fail}{Action if element of \code{labels}
cannot be interpreted. Choices are \code{"error"} (the default),
\code{"warn"}, and \code{"silent"}.}
}
\value{
Factor with the same length as \code{labels}.
}
\description{
Define a factor level representing an open cohort.
Replace existing cohorts where necessary.
\itemize{
\item \code{cohort_set_open_left()} defines a cohort with no lower limit.
\item \code{cohort_set_open_right()} defines a cohort with no upper limit.
}
}
\section{Rules for interpreting inputs}{


Single-year cohorts:\tabular{lll}{
   \code{interpret_single} \tab \emph{Rule} \tab \emph{Example} \cr
   \code{"lower"} \tab \verb{"a" -> [a,a+1)} \tab \verb{"2020" -> [2020,2021)} \cr
   \code{"upper"} \tab \verb{"a" -> [a-1,a)} \tab \verb{"2020" -> [2019,2020)} \cr
}


Data providers typically use the "lower" convention
for calendar years (1 January to 31 December),
and the "upper" convention for non-calendar years
(e.g., 1 July to 30 June).

Multi-year cohorts:\tabular{lll}{
   \code{interpret_multi} \tab \emph{Rule} \tab \emph{Example} \cr
   \code{"include"} \tab \verb{"a-<a+n>" -> [a,a+n)} \tab \verb{"2020-2025" -> [2020,2025)} \cr
   \code{"exclude"} \tab \verb{"a-<a+n-1>" -> [a,a+n)} \tab \verb{"2020-2024" -> [2020,2025)} \cr
}


A two-value label cannot describe a one-year cohort.
With \code{interpret_multi = "include"}, \code{"2010-2011"} would be
\verb{[2010, 2011)} and \code{"2010-2010"} would be empty; both are
rejected. Use \code{"2010"}, or \code{"2010-2012"} for two years.
With \code{interpret_multi = "exclude"},
\code{"2010-2011"} is two years \verb{[2010, 2012)} and is accepted.
}

\examples{
labels <- c("2020-2024", "<2000", "2015")
cohort_set_open_left(labels, at = 2020)
cohort_set_open_left(labels, at = 2005)
cohort_set_open_left(c("2000-2004", "2010-2014"), at = 1990)

labels <- c("2020-2024", "2025-2029", "2030")
cohort_set_open_right(labels, at = 2030)
}
\seealso{
\itemize{
\item \code{\link[=age_set_open_right]{age_set_open_right()}} Specify age group open on right
\item \code{\link[=period_set_open_left]{period_set_open_left()}} Specify period open on left
\item \code{\link[=period_set_open_right]{period_set_open_right()}} Specify period open on right
\item \code{\link[=cohort_is_open_left]{cohort_is_open_left()}} Identify cohorts open on left
\item \code{\link[=cohort_is_open_right]{cohort_is_open_right()}} Identify cohorts open on right
}
}
