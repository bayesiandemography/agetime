% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/age_diagnose.R
\name{age_diagnose}
\alias{age_diagnose}
\alias{age_assert}
\alias{age_diagnose_values}
\alias{age_assert_values}
\title{Diagnose or Assert Age Groups}
\usage{
age_diagnose(
  labels,
  no_overlap = FALSE,
  no_gap = FALSE,
  no_total = FALSE,
  no_na = FALSE,
  has_zero = FALSE,
  has_open_right = FALSE,
  valid_life = FALSE,
  interpret_fail = c("error", "warn", "silent")
)

age_assert(
  labels,
  no_overlap = FALSE,
  no_gap = FALSE,
  no_total = FALSE,
  no_na = FALSE,
  has_zero = FALSE,
  has_open_right = FALSE,
  valid_life = FALSE,
  interpret_fail = c("error", "warn", "silent")
)

age_diagnose_values(
  labels,
  no_overlap = FALSE,
  no_gap = FALSE,
  no_total = FALSE,
  no_na = FALSE,
  has_zero = FALSE,
  has_open_right = FALSE,
  valid_life = FALSE,
  interpret_fail = c("error", "warn", "silent")
)

age_assert_values(
  labels,
  no_overlap = FALSE,
  no_gap = FALSE,
  no_total = FALSE,
  no_na = FALSE,
  has_zero = FALSE,
  has_open_right = FALSE,
  valid_life = FALSE,
  interpret_fail = c("error", "warn", "silent")
)
}
\arguments{
\item{labels}{Vector of age group labels.}

\item{no_overlap}{Check that no age groups overlap.
Default is \code{FALSE} (don't check).}

\item{no_gap}{Check that all ages between the
youngest and oldest age groups are included.
Default is \code{FALSE} (don't check).}

\item{no_total}{Check that there is no total category.
Default is \code{FALSE} (don't check).}

\item{no_na}{Check that there is no \code{NA} category.
Default is \code{FALSE} (don't check).}

\item{has_zero}{Check that at least one
age group has a lower limit of zero.
Default is \code{FALSE} (don't check).}

\item{has_open_right}{Check that at least one age
group is open on the right (has no upper limit).
Default is \code{FALSE} (don't check).}

\item{valid_life}{Check that all labels are
valid for an abridged life table.
Default is \code{FALSE} (don't check).}

\item{interpret_fail}{Action if element of \code{labels}
cannot be interpreted. Choices are \code{"error"} (the default),
\code{"warn"}, and \code{"silent"}.}
}
\value{
\itemize{
\item \code{age_diagnose()} and \code{age_diagnose_values()} return a list with
a logical flag called \code{ok} and a \link[tibble:tibble]{tibble}
called \code{details}.
\item \code{age_assert()} and \code{age_assert_values()} return \code{labels}
invisibly, or raise an error.
}
}
\description{
\code{age_diagnose()} reports whether age group labels
meet conditions.

\code{age_assert()} throws an error if conditions are not met.

Conditions include:
\itemize{
\item Not overlapping
\item Not having gaps
\item Having totals, NAs, or zeros
\item Having open age groups
\item Valid for an abridged life table
}

When \code{labels} is a factor, these functions check \strong{levels},
including unused levels. Use \code{\link[=age_diagnose_values]{age_diagnose_values()}} or
\code{\link[=age_assert_values]{age_assert_values()}} to check only observed values (useful
with grouped data) while still returning the original factor
from \code{age_assert_values()}.
}
\section{Abridged and complete life tables}{

\itemize{
\item An abridged life table uses age groups \code{"0"}
and \code{"1-4"}, followed by 5-year age groups \code{"5-9"}, \code{"10-14"}, ...
\item A complete life table uses single-year age groups
\code{"0"}, \code{"1"}, \code{"2"}, ...
\item Both types of life table usually have an open age group
such as \code{"85+"} or \code{"100+"}.
}
}

\examples{
lab <- age_labels_life()
lab

## get info on everything
age_diagnose(
  labels = lab,
  no_overlap = TRUE,
  no_gap = TRUE,
  no_total = TRUE,
  no_na = TRUE,
  has_zero = TRUE,
  has_open_right = TRUE,
  valid_life = TRUE
)

## throw error if overlap or gap
age_assert(
  labels = lab,
  no_overlap = TRUE,
  no_gap = TRUE
)

## factor with unused overlapping level
fac <- factor("0-4", levels = c("0-4", "3-7"))
age_diagnose(fac, no_overlap = TRUE)$ok
age_diagnose_values(fac, no_overlap = TRUE)$ok
}
\seealso{
\itemize{
\item \code{\link[=age_diagnose_values]{age_diagnose_values()}} Check observed values only
\item \code{\link[=period_diagnose]{period_diagnose()}} Period equivalent of \code{age_diagnose()}
\item \code{\link[=cohort_diagnose]{cohort_diagnose()}} Cohort equivalent of \code{age_diagnose()}
}
}
