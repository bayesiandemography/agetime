% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/period_to.R
\name{period_to_one}
\alias{period_to_one}
\alias{period_to_five}
\alias{period_to_ten}
\alias{period_to_life}
\alias{period_to_labor}
\title{Convert Period Group Labels to Specialized Format}
\usage{
period_to_one(
  x,
  lower_first = NULL,
  lower_last = NULL,
  x_one = c("lower", "upper"),
  x_multi = c("include", "exclude"),
  x_fail = c("error", "warn", "silent")
)

period_to_five(
  x,
  lower_first = NULL,
  lower_last = NULL,
  open = TRUE,
  include_total = NULL,
  include_na = NULL,
  x_fail = c("error", "warn", "silent")
)

period_to_ten(
  x,
  lower_first = NULL,
  lower_last = NULL,
  open = TRUE,
  include_total = NULL,
  include_na = NULL,
  x_fail = c("error", "warn", "silent")
)

period_to_life(
  x,
  lower_last = NULL,
  include_total = NULL,
  include_na = NULL,
  x_fail = c("error", "warn", "silent")
)

period_to_labor(
  x,
  period_work = 20,
  period_retire = 65,
  include_total = NULL,
  include_na = NULL,
  x_fail = c("error", "warn", "silent")
)
}
\arguments{
\item{lower_first}{Lower limit of first period.}

\item{lower_last}{Lower limit of last period.}

\item{x_one}{How to interpret
labels in \code{x} that describe one-year periods.
Choices are \code{"lower"} (the default) and \code{"upper"}.}

\item{x_multi}{How to interpret
labels in \code{x} that describe multi-year periods.
Choices are \code{"include"} (the default) and
\code{"exclude"}.}

\item{include_total}{Whether to include a
\code{"Total"} category.}

\item{include_na}{Whether to include
an \code{NA} category.}
}
\value{
If \code{x} is a factor, then the return value is
a factor; otherwise it is a character vector.
}
\description{
Modify a set of existing period group labels
so that it conforms to a specialised format.
\itemize{
\item \code{period_to_one} Single-year period groups
\item \code{period_to_five} Five-year period groups
\item \code{period_to_ten} Ten-year period groups
}
}
\examples{
x <- c("2001-2003", "2007-2008", "2005"))
period_to_five(x, lower_first = 2000, lower_last = 2005)
period_to_five(x, lower_first = 2001, lower_last = 2006)
period_to_ten(x, lower_first = 1990, lower_last = 2010)
}
