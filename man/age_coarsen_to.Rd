% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/age_coarsen.R
\name{age_coarsen_to}
\alias{age_coarsen_to}
\title{Coarsen Age Groups to a Target Classification}
\usage{
age_coarsen_to(labels, to, interpret_fail = c("error", "warn", "silent"))
}
\arguments{
\item{labels}{Vector of age group labels.}

\item{to}{Vector of age group labels giving the target classification.}

\item{interpret_fail}{Action if an element of \code{labels} or \code{to}
cannot be interpreted. Choices are \code{"error"} (the default),
\code{"warn"}, and \code{"silent"}.}
}
\value{
Factor with the same length as \code{labels}.
}
\description{
Recode age group labels so they match a second set of labels.
}
\details{
Every interval in \code{labels} must lie in exactly one interval in \code{to}.
Intervals in \code{to} must not overlap. Gaps in \code{to} are allowed if
nothing in \code{labels} falls in them.

If \code{labels} is a factor, its levels are modified
along with its elements.
The return value is always a factor. Levels are the unique
labels in \code{to}, including unused labels.
}
\examples{
labels <- c("0", "1", "5", "10", "11")
to <- c("0-4", "5-9", "10-14")
age_coarsen_to(labels, to)

## unused labels in `to` are kept as levels
age_coarsen_to(c("0", "1"), to)
}
\seealso{
\itemize{
\item \code{\link[=age_coarsen]{age_coarsen()}} Coarsen using break points
\item \code{\link[=age_mapping]{age_mapping()}} Inspect the relationship between two sets of labels
\item \code{\link[=period_coarsen_to]{period_coarsen_to()}} Period equivalent of \code{age_coarsen_to()}
\item \code{\link[=cohort_coarsen_to]{cohort_coarsen_to()}} Cohort equivalent of \code{age_coarsen_to()}
}
}
