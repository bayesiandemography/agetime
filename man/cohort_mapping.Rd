% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cohort_mapping.R
\name{cohort_mapping}
\alias{cohort_mapping}
\title{Mapping Between Cohort Labels}
\usage{
cohort_mapping(
  labels,
  y = NULL,
  relation = c("equals", "contains", "is-contained-in", "overlaps-with"),
  format = c("tibble", "matrix"),
  interpret_single = c("lower", "upper"),
  interpret_range = c("include", "exclude"),
  interpret_fail = c("error", "warn", "silent")
)
}
\arguments{
\item{labels}{Vector of cohort labels.}

\item{y}{Vector of cohort labels. If
no value supplied, \code{labels} is mapped onto itself.}

\item{relation}{Relationship between
labels. Choices are \code{"equals"} (the default),
\code{"contains"}, \code{"is-contained-in"}, and \code{"overlaps-with"}.
See below for details and examples.}

\item{format}{Format of
return value. Choices are \code{"tibble"}
(the default) or \code{"matrix"}.}

\item{interpret_single}{Whether labels
for one-year cohorts are based on
lower or upper limit of period.
Default is \code{"lower"}.}

\item{interpret_range}{Whether
labels for multi-year periods
include or exclude final
year of period.
Default is \code{"include"}.}

\item{interpret_fail}{Action if element of \code{labels}
cannot be parsed: \code{"error"} (the default),
\code{"warn"}, or \code{"silent"}.}
}
\value{
\link[tibble:tibble]{Tibble} or matrix, depending on \code{format}.
}
\description{
Create a mapping between cohort labels. A mapping
depicts a relationship between the labels of \code{labels}
and the labels of \code{y}. The types of relationship
that can be mapped are:
\itemize{
\item "labels equals y"
\item "labels contains y"
\item "labels is contained in y"
\item "labels overlaps with y".
}
}
\details{
If no value for \code{y} is supplied,
\code{labels} is mapped onto itself.

Tibbles produced by \code{cohort_mapping()} are sparse
in that they only include matches. Matrices
produced by \code{cohort_mapping()} are dense in that
they include matches and non-matches. See the
example below.
}
\section{The \code{relation} argument}{
\tabular{ll}{
   \code{relation} \tab Endpoints of \code{labels} and \code{y} \cr
   \code{"equals"} \tab Endpoints equal \cr
   \code{"contains"} \tab Endpoints of \code{y} inside endpoints of \code{labels} \cr
   \code{"is-contained-in"} \tab Endpoints of \code{labels} inside endpoints of \code{y} \cr
   \code{"overlaps-with"} \tab Endpoint of \code{labels} in \code{y}, or reverse \cr
}
}

\examples{
labels <- c("2020-2025", "2030", "2025-2027")
y <- c("2025-2030", "2020-2025", "2026-2034")
cohort_mapping(labels = labels, y = y)
cohort_mapping(labels = labels, y = y, format = "matrix")
cohort_mapping(labels = labels, y = y, relation = "contains")
cohort_mapping(labels = labels, y = y, relation = "is-contained-in")
cohort_mapping(labels = labels, y = y, relation = "overlaps-with")

# sparse tibble vs dense matrix
labels <- c("2020-2025", "2030-2035")
y <- c("2020-2025", "<2025")
cohort_mapping(labels = labels, y = y) # one match
cohort_mapping(labels = labels, y = y, format = "matrix")

# mapping 'labels' on to itself
labels <- c("2020--2025", "2020-2025", "<2030")
cohort_mapping(labels)
}
\seealso{
\itemize{
\item \code{\link[=parsing_cohort_labels]{parsing_cohort_labels()}} Interpretation details for cohort labels
\item \code{\link[=age_mapping]{age_mapping()}} Age equivalent of \code{cohort_mapping()}
\item \code{\link[=period_mapping]{period_mapping()}} Period equivalent of \code{cohort_mapping()}
}
}
