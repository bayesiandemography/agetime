% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cohort_mapping.R
\name{cohort_mapping}
\alias{cohort_mapping}
\title{Mapping Between Cohort Labels}
\usage{
cohort_mapping(
  labels_x,
  labels_y = NULL,
  relation = c("equals", "contains", "is-contained-in", "overlaps-with"),
  format = c("tibble", "matrix"),
  name_x = "x",
  name_y = "y",
  interpret_single = c("lower", "upper"),
  interpret_multi = c("include", "exclude"),
  interpret_fail = c("error", "warn", "silent")
)
}
\arguments{
\item{labels_x}{Vector of cohort labels.}

\item{labels_y}{Vector of cohort labels. If
no value supplied, \code{labels_x} is mapped onto itself.}

\item{relation}{Relationship between
labels. Choices are \code{"equals"} (the default),
\code{"contains"}, \code{"is-contained-in"}, and \code{"overlaps-with"}.
See below for details and examples.}

\item{format}{Format of
return value. Choices are \code{"tibble"}
(the default) or \code{"matrix"}.}

\item{name_x, name_y}{Names for the two sides of the mapping
in the return value. Defaults are \code{"x"} and \code{"y"}.
Applied to tibble columns and to matrix dimnames.}

\item{interpret_single}{How to interpret
labels for single-year cohorts.
Choices are \code{"lower"} (the default) and \code{"upper"}.
See below for details.}

\item{interpret_multi}{How to interpret
labels for multi-year cohorts.
Choices are \code{"include"} (the default) and
\code{"exclude"}. See below for details.}

\item{interpret_fail}{Action if element of \code{labels}
cannot be interpreted. Choices are \code{"error"} (the default),
\code{"warn"}, and \code{"silent"}.}
}
\value{
\link[tibble:tibble]{Tibble} or matrix,
depending on the value of \code{format}.
}
\description{
Create a mapping between cohort labels. A mapping
depicts a relationship between \code{labels_x} and \code{labels_y}.
The types of relationship that can be mapped are:
\itemize{
\item "\code{labels_x} equals \code{labels_y}"
\item "\code{labels_x} contains \code{labels_y}"
\item "\code{labels_x} is contained in \code{labels_y}"
\item "\code{labels_x} overlaps with \code{labels_y}".
}
}
\details{
If no value for \code{labels_y} is supplied,
\code{labels_x} is mapped onto itself.

Tibbles produced by \code{cohort_mapping()} are sparse
in that they only include matches. Matrices
produced by \code{cohort_mapping()} are dense in that
they include matches and non-matches. See the
example below.
}
\section{The \code{relation} argument}{
\tabular{ll}{
   \code{relation} \tab Endpoints of \code{labels_x} and \code{labels_y} \cr
   \code{"equals"} \tab Endpoints equal \cr
   \code{"contains"} \tab Endpoints of \code{labels_y} inside endpoints of \code{labels_x} \cr
   \code{"is-contained-in"} \tab Endpoints of \code{labels_x} inside endpoints of \code{labels_y} \cr
   \code{"overlaps-with"} \tab Endpoint of \code{labels_x} in \code{labels_y}, or reverse \cr
}
}

\section{Rules for interpreting inputs}{


Single-year cohorts:\tabular{lll}{
   \code{interpret_single} \tab \emph{Rule} \tab \emph{Example} \cr
   \code{"lower"} \tab \verb{"a" -> [a,a+1)} \tab \verb{"2020" -> [2020,2021)} \cr
   \code{"upper"} \tab \verb{"a" -> [a-1,a)} \tab \verb{"2020" -> [2019,2020)} \cr
}


Data providers typically use the "lower" convention
for calendar years (1 January to 31 December),
and the "upper" convention for non-calendar years
(e.g., 1 July to 30 June).

Multi-year cohorts:\tabular{lll}{
   \code{interpret_multi} \tab \emph{Rule} \tab \emph{Example} \cr
   \code{"include"} \tab \verb{"a-<a+n>" -> [a,a+n)} \tab \verb{"2020-2025" -> [2020,2025)} \cr
   \code{"exclude"} \tab \verb{"a-<a+n-1>" -> [a,a+n)} \tab \verb{"2020-2024" -> [2020,2025)} \cr
}


A two-value label cannot describe a one-year cohort.
With \code{interpret_multi = "include"}, \code{"2010-2011"} would be
\verb{[2010, 2011)} and is rejected; use \code{"2010"}, or \code{"2010-2012"}
for two years. With \code{interpret_multi = "exclude"},
\code{"2010-2011"} is two years \verb{[2010, 2012)} and is accepted.
}

\examples{
x <- c("2020-2025", "2030", "2025-2027")
y <- c("2025-2030", "2020-2025", "2026-2034")
cohort_mapping(labels_x = x, labels_y = y)
cohort_mapping(labels_x = x, labels_y = y, format = "matrix")
cohort_mapping(labels_x = x, labels_y = y, relation = "contains")
cohort_mapping(labels_x = x, labels_y = y, relation = "is-contained-in")
cohort_mapping(labels_x = x, labels_y = y, relation = "overlaps-with")

# sparse tibble vs dense matrix
x <- c("2020-2025", "2030-2035")
y <- c("2020-2025", "<2025")
cohort_mapping(labels_x = x, labels_y = y) # one match
cohort_mapping(labels_x = x, labels_y = y, format = "matrix")

# map labels_x onto itself
x <- c("2020--2025", "2020-2025", "<2030")
cohort_mapping(x)
}
\seealso{
\itemize{
\item \code{\link[=cohort_coarsen_to]{cohort_coarsen_to()}} Recode labels into another classification
\item \code{\link[=age_mapping]{age_mapping()}} Age equivalent of \code{cohort_mapping()}
\item \code{\link[=period_mapping]{period_mapping()}} Period equivalent of \code{cohort_mapping()}
}
}
