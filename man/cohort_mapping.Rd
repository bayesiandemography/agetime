% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cohort_mapping.R
\name{cohort_mapping}
\alias{cohort_mapping}
\title{Mapping Between Cohort Labels}
\usage{
cohort_mapping(
  x,
  y = NULL,
  relation = c("equals", "contains", "is-contained-in", "overlaps-with"),
  format = c("tibble", "matrix"),
  x_one = c("lower", "upper"),
  x_multi = c("include", "exclude"),
  x_fail = c("error", "warn", "silent")
)
}
\arguments{
\item{x}{Vector of cohort labels.}

\item{y}{Vector of cohort labels. If
no value supplied, \code{x} is mapped onto itself.}

\item{relation}{Relationship between
labels. Choices are \code{"equals"} (the default),
\code{"contains"}, \code{"is-contained-in"}, and \code{"overlaps-with"}.
See below for details and examples.}

\item{format}{Format of
return value. Choices are \code{"tibble"}
(the default) or \code{"matrix"}.}

\item{x_one}{Whether labels
for one-year cohorts are based on
lower or upper limit of period.
Default is \code{"lower"}.}

\item{x_multi}{Whether
labels for multi-year periods
include or exclude final
year of period.
Default is \code{"include"}.}

\item{x_fail}{Action if element of \code{x}
cannot be parsed: \code{"error"} (the default),
\code{"warn"}, or \code{"silent"}.}
}
\value{
\link[tibble:tibble]{Tibble} or matrix, depending on \code{format}.
}
\description{
Create a mapping between cohort labels. The mapping is
based on one of four types of relationship:
}
\details{
If no value for \code{y} is supplied,
\code{x} is mapped onto itself.

Tibbles produced by \code{cohort_mapping()} are sparse, while matrices are dense. See the example below.
}
\section{The \code{relation} argument}{
\tabular{ll}{
   \code{relation} \tab Endpoints of \code{x} and \code{y} \cr
   \code{"equals"} \tab \code{cohort_lower(x) == cohort_lower(y) & cohort_upper(x) == cohort_upper(y)} \cr
   \code{"contains"} \tab \code{cohort_lower(x) <= cohort_lower(y) & cohort_upper(y) <= cohort_upper(x)} \cr
   \code{"is-contained-in"} \tab \code{cohort_lower(y) <= cohort_lower(x) & cohort_upper(x) <= cohort_upper(y)} \cr
   \code{"overlaps-with"} \tab \verb{(cohort_lower(y) <= cohort_lower(x) < cohort_upper(y))} | \verb{(cohort_lower(y) <= cohort_upper(x) < cohort_upper(y))} \cr
}
}

\examples{
x <- c("2020-2025", "2030", "2025-2027")
y <- c("2025-2030", "2020-2025", "2026-2034")
cohort_mapping(x = x, y = y)
cohort_mapping(x, format = "matrix")
cohort_mapping(x = x, y = y, relation = "contains")
cohort_mapping(x = x, y = y, relation = "is-contained-in")
cohort_mapping(x = x, y = y, relation = "overlaps-with")

# sparse tibble vs dense matrix
x <- c("2020-2025", "2030-2035")
y <- c("2020-2025", "<2025")
cohort_mapping(x = x, y = y) # one match
cohort_mapping(x = x, y = y, format = "matrix") # one match and three non-matches

# mapping 'x' on to itself
x <- c("2020--2025", "2020-2025", "<2030")
cohort_mapping(x)
}
\seealso{
\itemize{
\item \code{\link[=parsing_cohort_labels]{parsing_cohort_labels()}} Details for \code{x_one}, \code{x_multi}, and \code{x_fail}
\item \code{\link[=age_mapping]{age_mapping()}} Age equivalent of \code{cohort_mapping()}
\item \code{\link[=period_mapping]{period_mapping()}} Period equivalent of \code{cohort_mapping()}
}
}
