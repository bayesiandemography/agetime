% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cohort_mapping.R
\name{cohort_mapping}
\alias{cohort_mapping}
\title{Mapping Between Cohort Labels}
\usage{
cohort_mapping(
  x,
  y = NULL,
  relation = c("equals", "contains", "contained", "overlaps"),
  return_val = c("data.frame", "matrix"),
  parse_one = c("lower", "upper"),
  parse_multi = c("include", "exclude"),
  parse_fail = c("error", "warn", "silent")
)
}
\arguments{
\item{x}{Vector of cohort labels.}

\item{y}{Vector of cohort labels. If
no value supplied, \code{x} is mapped onto itself.}

\item{relation}{Relationship between
labels. The choices are \code{"equals"} (the default),
\code{"contains"}, \code{"contained"}, and \code{"overlaps"}.
See below for details and examples.}

\item{return_val}{The format of the
return value. The choices are \code{"data.frame"}
(the default) or \code{"matrix"}.}

\item{parse_one}{Whether labels
for one-year cohorts are based on the
lower or upper limit of the period.
Default is \code{"lower"}.}

\item{parse_multi}{Whether
labels for multi-year periods
include or exclude the final
year of the period.
Default is \code{"include"}.}

\item{parse_fail}{Action if a label
cannot be interpreted. Choices are
\code{"error"} (the default), \code{"warn"},
and \code{"silent"}.}
}
\value{
A data.frame or matrix
}
\description{
Create a mapping between cohort labels. The mapping is
based on one of four types of relationship:
}
\details{
If no value for \code{y} is supplied,
\code{x} is mapped onto itself.
}
\section{The \code{relation} argument}{
\tabular{ll}{
   \code{relation} \tab Endpoints of \code{x} and \code{y} \cr
   \code{"equals"} \tab \code{cohort_lower(x) == cohort_lower(y) & cohort_upper(x) == cohort_upper(y)} \cr
   \code{"contains"} \tab \code{cohort_lower(x) <= cohort_lower(y) & cohort_upper(y) <= cohort_upper(x)} \cr
   \code{"contained"} \tab \code{cohort_lower(y) <= cohort_lower(x) & cohort_upper(x) <= cohort_upper(y)} \cr
   \code{"overlaps"} \tab \verb{(cohort_lower(y) <= cohort_lower(x) < cohort_upper(y))} | \verb{(cohort_lower(y) <= cohort_upper(x) < cohort_upper(y))} \cr
}
}

\examples{
x <- c("2020-2025", "2030", "2025-2027")
y <- c("2025-2030", "2020-2025", "2026-2034")
cohort_mapping(x = x, y = y)
cohort_mapping(x, return_val = "matrix")
cohort_mapping(x = x, y = y, relation = "contains")
cohort_mapping(x = x, y = y, relation = "contained")
cohort_mapping(x = x, y = y, relation = "overlaps")
}
