% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/period_diagnose.R
\name{period_diagnose}
\alias{period_diagnose}
\alias{period_assert}
\title{Diagnose or Assert Periods}
\usage{
period_diagnose(
  labels,
  no_overlap = FALSE,
  no_gap = FALSE,
  no_total = FALSE,
  no_na = FALSE,
  has_open_left = FALSE,
  has_open_right = FALSE,
  interpret_single = c("lower", "upper"),
  interpret_multi = c("include", "exclude"),
  interpret_fail = c("error", "warn", "silent")
)

period_assert(
  labels,
  no_overlap = FALSE,
  no_gap = FALSE,
  no_total = FALSE,
  no_na = FALSE,
  has_open_left = FALSE,
  has_open_right = FALSE,
  interpret_single = c("lower", "upper"),
  interpret_multi = c("include", "exclude"),
  interpret_fail = c("error", "warn", "silent")
)
}
\arguments{
\item{labels}{Vector of period labels.}

\item{no_overlap}{Check that no periods overlap.
Default is \code{FALSE} (don't check).}

\item{no_gap}{Check that all periods between
the earliest and latest periods are included.
Default is \code{FALSE} (don't check).}

\item{no_total}{Check that there is no total category.
Default is \code{FALSE} (don't check).}

\item{no_na}{Check that there is no \code{NA} category.
Default is \code{FALSE} (don't check).}

\item{has_open_left}{Check that at least one period
is open on the left (has no lower limit).
Default is \code{FALSE} (don't check).}

\item{has_open_right}{Check that at least one period
is open on the right (has no upper limit).
Default is \code{FALSE} (don't check).}

\item{interpret_single}{How to interpret
labels for single-year periods.
Choices are \code{"lower"} (the default) and \code{"upper"}.
See below for details.}

\item{interpret_multi}{How to interpret
labels for multi-year periods.
Choices are \code{"include"} (the default) and
\code{"exclude"}. See below for details.}

\item{interpret_fail}{Action if element of \code{labels}
cannot be interpreted. Choices are \code{"error"} (the default),
\code{"warn"}, and \code{"silent"}.}
}
\value{
\itemize{
\item \code{period_diagnose()} returns a list with a logical flag
called \code{ok} and a \link[tibble:tibble]{tibble} called \code{details}.
\item \code{period_assert()} returns \code{labels} invisibly,
or raises an error.
}
}
\description{
\code{period_diagnose()} reports whether period labels
meet conditions.

\code{period_assert()} throws an error if conditions are not met.

Conditions include:
\itemize{
\item Not overlapping
\item Not having gaps
\item Having totals or NAs
\item Having open periods
}
}
\section{Rules for interpreting inputs}{


Single-year periods:\tabular{lll}{
   \code{interpret_single} \tab \emph{Rule} \tab \emph{Example} \cr
   \code{"lower"} \tab \verb{"a" -> [a,a+1)} \tab \verb{"2020" -> [2020,2021)} \cr
   \code{"upper"} \tab \verb{"a" -> [a-1,a)} \tab \verb{"2020" -> [2019,2020)} \cr
}


Data providers typically use the "lower" convention
for calendar years (1 January to 31 December),
and the "upper" convention for non-calendar years
(e.g., 1 July to 30 June).

Multi-year periods:\tabular{lll}{
   \code{interpret_multi} \tab \emph{Rule} \tab \emph{Example} \cr
   \code{"include"} \tab \verb{"a-<a+n>" -> [a,a+n)} \tab \verb{"2020-2025" -> [2020,2025)} \cr
   \code{"exclude"} \tab \verb{"a-<a+n-1>" -> [a,a+n)} \tab \verb{"2020-2024" -> [2020,2025)} \cr
}
}

\examples{
lab <- period_labels_five(
  lower_first = 2020,
  lower_last = 2030
)
lab

## get info on everything
period_diagnose(
  labels = lab,
  no_overlap = TRUE,
  no_gap = TRUE,
  no_total = TRUE,
  no_na = TRUE
)

## throw error if overlap or gap
period_assert(
  labels = lab,
  no_overlap = TRUE,
  no_gap = TRUE
)
}
\seealso{
\itemize{
\item \code{\link[=age_diagnose]{age_diagnose()}} Age equivalent of \code{period_diagnose()}
\item \code{\link[=cohort_diagnose]{cohort_diagnose()}} Cohort equivalent of \code{period_diagnose()}
}
}
