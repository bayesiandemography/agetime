% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/period_extend.R
\name{period_extend}
\alias{period_extend}
\title{Extend a Set of Periods}
\usage{
period_extend(
  labels,
  n = 1L,
  width = NULL,
  include_x = TRUE,
  interpret_single = c("lower", "upper"),
  interpret_range = c("include", "exclude"),
  interpret_fail = c("error", "warn", "silent")
)
}
\arguments{
\item{labels}{Vector of period labels.}

\item{n}{Number of periods to add.
Default is \code{1}.}

\item{width}{Width of the periods
to be added.}

\item{include_x}{Should the return value
include \code{labels}? Default is \code{TRUE}.}

\item{interpret_single}{How to interpret
labels in \code{labels} that describe one-year periods.
Choices are \code{"lower"} (the default) and \code{"upper"}.}

\item{interpret_range}{How to interpret
labels in \code{labels} that describe multi-year periods.
Choices are \code{"include"} (the default) and
\code{"exclude"}.}

\item{interpret_fail}{Action if element of \code{labels}
cannot be parsed: \code{"error"} (the default),
\code{"warn"}, or \code{"silent"}.}
}
\value{
Character vector or factor.
Length is \code{n}, or \code{length(labels) + n} when \code{include_x} is \code{TRUE}.
}
\description{
Add new periods at the end of \code{labels}.
}
\details{
By default, the width of the new periods
is derived from the last element of \code{labels},
but a value can be specified through the
\code{width} arugment.
}
\examples{
labels <- c("2020-2025", "2025-2030")
period_extend(labels, n = 2)
period_extend(labels, n = 2, width = 10)
period_extend(labels, n = 2, include_x = FALSE)
}
\seealso{
\itemize{
\item \code{\link[=parsing_period_labels]{parsing_period_labels()}} Interpretation details for period labels
\item \code{\link[=age_extend]{age_extend()}} Age equivalent of \code{period_extend()}
\item \code{\link[=cohort_extend]{cohort_extend()}} Cohort equivalent of \code{period_extend()}
}
}
