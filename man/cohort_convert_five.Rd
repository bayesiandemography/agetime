% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cohort_convert.R
\name{cohort_convert_five}
\alias{cohort_convert_five}
\alias{cohort_convert_ten}
\title{Convert to Equal-Length Cohorts}
\usage{
cohort_convert_five(
  x,
  offset = 0,
  x_one = c("lower", "upper"),
  x_multi = c("include", "exclude"),
  x_fail = c("error", "warn", "silent")
)

cohort_convert_ten(
  x,
  offset = 0,
  x_one = c("lower", "upper"),
  x_multi = c("include", "exclude"),
  x_fail = c("error", "warn", "silent")
)
}
\arguments{
\item{x}{A vector of cohort labels.}

\item{offset}{Parameter controlling
alignment of cohorts. Default is \code{0}.}

\item{x_one}{Whether labels
for one-year cohorts are based on the
lower or upper limit of the period.
Default is \code{"lower"}.}

\item{x_multi}{Whether
labels for multi-year periods
include or exclude the final
year of the period.
Default is \code{"include"}.}

\item{x_fail}{Action if a label
cannot be interpreted. Choices are
\code{"error"} (the default), \code{"warn"},
and \code{"silent"}.}
}
\value{
If \code{x} is a factor, then the return value is
a factor; otherwise it is a character vector.
}
\description{
Modify the cohorts used by \code{x}.
The new cohorts must contain
the old cohorts, and follow a regular
pattern:
\itemize{
\item \code{cohort_convert_five} Five-year cohorts
\item \code{cohort_convert_ten} Ten-year cohorts
}
}
\examples{
x <- c("2002-2004", "1987-1989", "2000", "Total")
cohort_convert_five(x)
cohort_convert_five(x, offset = 1)
cohort_convert_five(x, offset = 2)
cohort_convert_ten(x)
cohort_convert_ten(x, offset = 1)
cohort_convert_ten(x, offset = 2)
}
\seealso{
\code{\link[=cohort_convert]{cohort_convert()}} Convert to general cohorts
\code{\link[=age_convert_five]{age_convert_five()}} Age equivalent of \code{cohort_convert_five()}
\code{\link[=age_convert_ten]{age_convert_ten()}} Age equivalent of \code{cohort_convert_ten()}
\code{\link[=period_convert_five]{period_convert_five()}} Period equivalent of \code{cohort_convert_five()}
\code{\link[=period_convert_ten]{period_convert_ten()}} Period equivalent of \code{cohort_convert_ten()}
\code{\link[=cohort_complete]{cohort_complete()}} Add levels for intermediate cohorts
}
