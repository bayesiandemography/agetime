% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/period_convert.R
\name{period_convert}
\alias{period_convert}
\title{Convert to New Periods}
\usage{
period_convert(
  x,
  breaks,
  x_one = c("lower", "upper"),
  x_multi = c("include", "exclude"),
  x_fail = c("error", "warn", "silent")
)
}
\arguments{
\item{x}{Vector of period labels.}

\item{breaks}{Boundaries between periods.
A numeric vector.}

\item{x_one}{How to interpret
labels in \code{x} that describe one-year periods.
Choices are \code{"lower"} (the default) and \code{"upper"}.}

\item{x_multi}{How to interpret
labels in \code{x} that describe multi-year periods.
Choices are \code{"include"} (the default) and
\code{"exclude"}.}

\item{x_fail}{What to do if a
label in \code{x} cannot be interpreted.
Choices are
\code{"error"} (the default), \code{"warn"},
and \code{"silent"}.}
}
\value{
If \code{x} is a factor, then the return value is
a factor; otherwise it is a character vector.
}
\description{
Modify the periods used by \code{x}. The
the new periods must
contain the old ones.
}
\examples{
x <- c("2001-2004", "1987-1989", "2000", "2005-2010")
period_convert(breaks = c(1990, 2000, 2005, 2015))
}
\seealso{
\code{\link[=period_convert_five]{period_convert_five()}} Convert to 5-year periods
\code{\link[=period_convert_ten]{period_convert_ten()}} Convert to 10-year periods
\code{\link[=age_convert]{age_convert()}} Age group equivalent of \code{period_convert()}
\code{\link[=cohort_convert]{cohort_convert()}} Cohort equivalent of \code{period_convert()}
}
