% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/period_coarsen.R
\name{period_coarsen}
\alias{period_coarsen}
\title{Coarsen Periods}
\usage{
period_coarsen(
  labels,
  breaks,
  open_left = NULL,
  open_right = NULL,
  interpret_single = c("lower", "upper"),
  interpret_multi = c("include", "exclude"),
  interpret_fail = c("error", "warn", "silent")
)
}
\arguments{
\item{labels}{Vector of period labels.}

\item{breaks}{Boundaries between periods.
A numeric vector.}

\item{open_left}{Whether to include a period that is open on the left.
Optional. See below for details.}

\item{open_right}{Whether to include a period that is open on the right.
Optional. See below for details.}

\item{interpret_single}{How to interpret
labels for single-year periods.
Choices are \code{"lower"} (the default) and \code{"upper"}.
See below for details.}

\item{interpret_multi}{How to interpret
labels for multi-year periods.
Choices are \code{"include"} (the default) and
\code{"exclude"}. See below for details.}

\item{interpret_fail}{Action if element of \code{labels}
cannot be interpreted. Choices are \code{"error"} (the default),
\code{"warn"}, and \code{"silent"}.}
}
\value{
Factor with the same length as \code{labels}.
}
\description{
Define new periods that contain existing periods.
}
\details{
The new periods cannot split existing periods,
and are typically wider than the existing periods.

If \code{labels} is a factor, its levels are modified
along with its elements.
}
\section{The \code{open_left} argument}{


A period is open on the left if it has no lower limit,
e.g. \code{"<2000"}.

By default, the return value has a period
that is open on the left if and only if \code{labels} does.
The full range of options is:\tabular{lll}{
   \code{open_left} \tab \code{labels} has open on left \tab Return value has open on left \cr
   \code{NULL} (default) \tab Yes \tab Yes \cr
   \code{NULL} (default) \tab No \tab No \cr
   \code{TRUE} \tab Yes \tab Yes \cr
   \code{TRUE} \tab No \tab Yes \cr
   \code{FALSE} \tab Yes \tab \emph{Error} \cr
   \code{FALSE} \tab No \tab No \cr
}
}

\section{The \code{open_right} argument}{


A period is open on the right if it has no upper limit,
e.g. \code{"2000+"}.

By default, the return value has a period
that is open on the right if and only if \code{labels} does.
The full range of options is:\tabular{lll}{
   \code{open_right} \tab \code{labels} has open on right \tab Return value has open on right \cr
   \code{NULL} (default) \tab Yes \tab Yes \cr
   \code{NULL} (default) \tab No \tab No \cr
   \code{TRUE} \tab Yes \tab Yes \cr
   \code{TRUE} \tab No \tab Yes \cr
   \code{FALSE} \tab Yes \tab \emph{Error} \cr
   \code{FALSE} \tab No \tab No \cr
}
}

\section{Rules for interpreting inputs}{


Single-year periods:\tabular{lll}{
   \code{interpret_single} \tab \emph{Rule} \tab \emph{Example} \cr
   \code{"lower"} \tab \verb{"a" -> [a,a+1)} \tab \verb{"2020" -> [2020,2021)} \cr
   \code{"upper"} \tab \verb{"a" -> [a-1,a)} \tab \verb{"2020" -> [2019,2020)} \cr
}


Data providers typically use the "lower" convention
for calendar years (1 January to 31 December),
and the "upper" convention for non-calendar years
(e.g., 1 July to 30 June).

Multi-year periods:\tabular{lll}{
   \code{interpret_multi} \tab \emph{Rule} \tab \emph{Example} \cr
   \code{"include"} \tab \verb{"a-<a+n>" -> [a,a+n)} \tab \verb{"2020-2025" -> [2020,2025)} \cr
   \code{"exclude"} \tab \verb{"a-<a+n-1>" -> [a,a+n)} \tab \verb{"2020-2024" -> [2020,2025)} \cr
}


A two-value label cannot describe a one-year period.
With \code{interpret_multi = "include"}, \code{"2010-2011"} would be
\verb{[2010, 2011)} and \code{"2010-2010"} would be empty; both are
rejected. Use \code{"2010"}, or \code{"2010-2012"} for two years.
With \code{interpret_multi = "exclude"},
\code{"2010-2011"} is two years \verb{[2010, 2012)} and is accepted.
}

\examples{
labels <- c("2001-2004", "1987-1989", "2000", "2005-2010")
period_coarsen(labels, breaks = c(1970, 2000, 2005, 2015))

## let inclusion of open period depend on labels
labels_closed <- c("2001-2004", "2005-2010")
period_coarsen(labels_closed, breaks = c(2000, 2010, 2020))
labels_open <- c("<1970", "2001-2004", "2020+")
period_coarsen(labels_open, breaks = c(1970, 2000, 2010, 2020))

## insist on an open period
period_coarsen(
  labels_closed,
  breaks = c(2000, 2010, 2020),
  open_right = TRUE
)

}
\seealso{
\itemize{
\item \code{\link[=period_coarsen_to]{period_coarsen_to()}} Coarsen to an existing set of labels
\item \code{\link[=period_coarsen_five]{period_coarsen_five()}} Coarsen to 5-year periods
\item \code{\link[=period_coarsen_ten]{period_coarsen_ten()}} Coarsen to 10-year periods
\item \code{\link[=age_coarsen]{age_coarsen()}} Age group equivalent of \code{period_coarsen()}
\item \code{\link[=cohort_coarsen]{cohort_coarsen()}} Cohort equivalent of \code{period_coarsen()}
}
}
