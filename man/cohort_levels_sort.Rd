% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cohort_levels_sort.R
\name{cohort_levels_sort}
\alias{cohort_levels_sort}
\title{Sort Cohort Levels}
\usage{
cohort_levels_sort(
  x,
  decreasing = FALSE,
  x_one = c("lower", "upper"),
  x_multi = c("include", "exclude"),
  x_fail = c("error", "warn", "silent")
)
}
\arguments{
\item{x}{Vector of cohort labels.}

\item{decreasing}{Whether sort is
increasing or decreasing. Default
is \code{FALSE}.}

\item{x_one}{Whether labels
for one-year cohorts are based on the
lower or upper limit of the period.
Default is \code{"lower"}.}

\item{x_multi}{Whether
labels for multi-year periods
include or exclude the final
year of the period.
Default is \code{"include"}.}

\item{x_fail}{Action if element of \code{x}
cannot be parsed: \code{"error"} (the default),
\code{"warn"}, or \code{"silent"}.}
}
\value{
A factor, the same length as \code{x}.
}
\description{
Sort the levels of \code{x}.
}
\details{
If \code{x} is not a factor, and so
does not have levels,
convert it to a factor first.

Levels are sorted on their lower
limits. When there are ties,
upper limits are used. \code{NA}s come
second-to-last, and
totals come last.
}
\examples{
x <- c("2020-2025", "<1990", "Total", NA, "2025-2050")
cohort_levels_sort(x)
}
\seealso{
\code{\link[=age_levels_sort]{age_levels_sort()}} Age equivalent of \code{cohort_levels_sort()}
\code{\link[=period_levels_sort]{period_levels_sort()}} Period equivalent of \code{cohort_levels_sort()}
}
