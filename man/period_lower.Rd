% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/period_lower.R
\name{period_lower}
\alias{period_lower}
\alias{period_mid}
\alias{period_upper}
\alias{period_width}
\title{Lower Limits, Upper Limits, Widths,
and Midpoints of Periods}
\usage{
period_lower(
  x,
  x_one = c("lower", "upper"),
  x_multi = c("include", "exclude"),
  x_fail = c("error", "warn", "silent")
)

period_mid(
  x,
  x_one = c("lower", "upper"),
  x_multi = c("include", "exclude"),
  x_fail = c("error", "warn", "silent")
)

period_upper(
  x,
  x_one = c("lower", "upper"),
  x_multi = c("include", "exclude"),
  x_fail = c("error", "warn", "silent")
)

period_width(
  x,
  x_one = c("lower", "upper"),
  x_multi = c("include", "exclude"),
  x_fail = c("error", "warn", "silent")
)
}
\arguments{
\item{x}{Vector of period labels.}

\item{x_one}{How to interpret
labels in \code{x} that describe one-year periods.
Choices are \code{"lower"} (the default) and \code{"upper"}.}

\item{x_multi}{How to interpret
labels in \code{x} that describe multi-year periods.
Choices are \code{"include"} (the default) and
\code{"exclude"}.}

\item{x_fail}{Action if element of \code{x}
cannot be parsed: \code{"error"} (the default),
\code{"warn"}, or \code{"silent"}.}
}
\value{
Numeric vector with same length as \code{x}.
}
\description{
Calculate lower limits, upper limits,
widths, and midpoints for periods.
}
\details{
Lower and upper limits can be used
to filter on periods.
See below for examples.
}
\section{Interpretation parameters}{


\code{x_one}, \code{x_multi}, and \code{x_fail}
control how \pkg{agetime} understands and processes
values in \code{x}.
\itemize{
\item \code{x_one}. How to interpret a one-year period
label such as \code{"2030"}. When \code{x_one} is \code{"lower"},
\pkg{agetime} assumes that the label
is based on the lower limit, so that
\code{"2030"} refers to a period such as
1 January 2030 to 1 January 2031.
When \code{x_one} is \code{"upper"},
\pkg{agetime} assumes that the label is based on the
upper limit, so that \code{"2030"} refers to a period
such as 30 June 2029 to 30 June 2030.
\item \code{x_multi}. How to interpret a multi-year period
label such as \code{"2030-2035"}.
When \code{x_multi} is \code{"include"},
\pkg{agetime} assumes that the label includes the upper limit,
so that \code{"2030-2035"} refers to a period such as
30 June 2030 to 30 June 2035.
When \code{x_multi} is \code{"exclude"},
\pkg{agetime} assumes that the label excludes
the upper limit, so that \code{"2030-2035"} refers to a
period such as 1 January 2030 to 1 January 2036.
\item \code{x_fail}. Action \pkg{agetime} should take
if it can't interpret a label. When \code{x_fail} is
\code{"error"}, \pkg{agetime} throws an error.
When \code{x_fail} is \code{"warn"},
\pkg{agetime} throws a warning. When \code{interpet_fail}
is \code{"silent"}, \pkg{agetime} ignores the label and carries on.
}
}

\examples{
x <- c("2025-2030", "2020-2025", "2030-2035")
period_lower(x)
period_upper(x)
period_width(x)
period_mid(x)

## use 'period_lower()' to filter on period
library(dplyr, warn.conflicts = FALSE)
df <- tribble(    ~period, ~count,
              "2020-2025",     20,
              "2025-2030",      5,
              "2030-2035",     11 )
df
df |> filter(period_lower(period) >= 2025)

## 'x_one' is "lower" (the default)
period_lower("2025")
period_upper("2025")
period_width("2025")

## 'x_one' is "upper"
period_lower("2025", x_one = "upper")
period_upper("2025", x_one = "upper")
period_width("2025", x_one = "upper")

## 'x_multi' is "include" (the default)
period_upper("2025-2030")
period_width("2025-2030")

## 'x_multi' is "exclude"
period_upper("2025-2030", x_multi = "exclude")
period_width("2025-2030", x_multi = "exclude")

## no action when 'x_fail' is "silent"
period_lower(c("2000-2005", "long time ago"),
             x_fail = "silent")
}
\seealso{
\code{\link[=age_lower]{age_lower()}} Age equivalent of \code{period_lower()}
\code{\link[=cohort_lower]{cohort_lower()}} Cohort equivalent of \code{period_lower()}
}
