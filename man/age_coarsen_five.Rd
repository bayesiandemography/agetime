% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/age_coarsen.R
\name{age_coarsen_five}
\alias{age_coarsen_five}
\alias{age_coarsen_ten}
\alias{age_coarsen_life}
\title{Coarsen to Specialised Age Groups}
\usage{
age_coarsen_five(
  labels,
  open_right = NULL,
  interpret_fail = c("error", "warn", "silent")
)

age_coarsen_ten(
  labels,
  open_right = NULL,
  interpret_fail = c("error", "warn", "silent")
)

age_coarsen_life(
  labels,
  open_right = NULL,
  interpret_fail = c("error", "warn", "silent")
)
}
\arguments{
\item{labels}{Vector of age group labels.}

\item{open_right}{Whether to include an age group that is
open on the right. Optional. See below for details.}

\item{interpret_fail}{Action if element of \code{labels}
cannot be interpreted. Choices are \code{"error"} (the default),
\code{"warn"}, and \code{"silent"}.}
}
\value{
Character vector or factor with the same length as \code{labels}.
}
\description{
Define new age groups that contain existing
age groups and that follow a regular
pattern:
\itemize{
\item \code{age_coarsen_five} Five-year age groups
\item \code{age_coarsen_ten} Ten-year age groups
\item \code{age_coarsen_life} Age groups used in 'abridged' life tables
}
}
\details{
The new age groups cannot split existing
age groups.

If \code{labels} is a factor, its levels are modified
along with its elements.
}
\section{Abridged and complete life tables}{

\itemize{
\item An 'abridged' life table uses age groups \code{"0"}
and \code{"1-4"}, followed by 5-year age groups \code{"5-9"}, \code{"10-14"}, ...
\item A 'complete' life table uses single-year age groups
\code{"0"}, \code{"1"}, \code{"2"}, ...
\item Both types of life table usually have an open age group
such as \code{"85+"} or \code{"100+"}.
}
}

\section{The \code{open_right} argument}{


An age group is open on the right if it has no upper limit,
e.g. \code{"100+"}.

By default, the return value has an open age group
if and only if \code{labels} does. The full range of options is:\tabular{lll}{
   \code{open_right} \tab \code{labels} has open on right \tab Return value has open on right \cr
   \code{NULL} (default) \tab Yes \tab Yes \cr
   \code{NULL} (default) \tab No \tab No \cr
   \code{TRUE} \tab Yes \tab Yes \cr
   \code{TRUE} \tab No \tab Yes \cr
   \code{FALSE} \tab Yes \tab \emph{Error} \cr
   \code{FALSE} \tab No \tab No \cr
}
}

\examples{
labels <- c("1-3", "16-19", "0", "31+", "total", "22")
age_coarsen_five(labels)
age_coarsen_ten(labels)
age_coarsen_life(labels)

labels_no_open <- c("1-3", "16-19", "0", "total", "22")
age_coarsen_five(labels_no_open)
age_coarsen_five(labels_no_open, open_right = TRUE)
}
\seealso{
\itemize{
\item \code{\link[=age_coarsen]{age_coarsen()}} Coarsen to general age groups
\item \code{\link[=period_coarsen_five]{period_coarsen_five()}} Period equivalent of \code{age_coarsen_five()}
\item \code{\link[=period_coarsen_ten]{period_coarsen_ten()}} Period equivalent of \code{age_coarsen_ten()}
\item \code{\link[=cohort_coarsen_five]{cohort_coarsen_five()}} Cohort equivalent of \code{age_coarsen_five()}
\item \code{\link[=cohort_coarsen_ten]{cohort_coarsen_ten()}} Cohort equivalent of \code{age_coarsen_ten()}
\item \code{\link[=age_fill]{age_fill()}} Add levels for intermediate age groups
}
}
