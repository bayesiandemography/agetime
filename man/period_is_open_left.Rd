% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/period_is_total.R
\name{period_is_open_left}
\alias{period_is_open_left}
\alias{period_is_open_right}
\title{Identify Open Periods}
\usage{
period_is_open_left(
  labels,
  interpret_single = c("lower", "upper"),
  interpret_multi = c("include", "exclude"),
  interpret_fail = c("error", "warn", "silent")
)

period_is_open_right(
  labels,
  interpret_single = c("lower", "upper"),
  interpret_multi = c("include", "exclude"),
  interpret_fail = c("error", "warn", "silent")
)
}
\arguments{
\item{labels}{Vector of period labels.}

\item{interpret_single}{How to interpret
labels for single-year periods.
Choices are \code{"lower"} (the default) and \code{"upper"}.
See below for details.}

\item{interpret_multi}{How to interpret
labels for multi-year periods.
Choices are \code{"include"} (the default) and
\code{"exclude"}. See below for details.}

\item{interpret_fail}{Action if element of \code{labels}
cannot be interpreted. Choices are \code{"error"} (the default),
\code{"warn"}, and \code{"silent"}.}
}
\value{
Logical vector with the same length as \code{labels}.
}
\description{
Find periods that are open on the left or right.
}
\details{
\itemize{
\item \code{period_is_open_left()} finds periods open on the left
(has no lower limit).
\item \code{period_is_open_right()} finds periods open on the right
(has no upper limit).
}
}
\section{Controlling how period labels are interpreted}{


Interpretation of single-year periods:\tabular{lll}{
   \code{interpret_single} \tab Rule \tab Example \cr
   \code{"lower"} \tab \verb{"a" -> [a,a+1)} \tab \verb{"2020" -> [2020,2021)} \cr
   \code{"upper"} \tab \verb{"a" -> [a-1,a)} \tab \verb{"2020" -> [2019,2020)} \cr
}


Data providers typically use the "lower" convention
for calendar years (1 January to 31 December),
and the "upper" convention for non-calendar years
(e.g., 1 July to 30 June).

Interpretation of multi-year periods:\tabular{lll}{
   \code{interpret_multi} \tab Rule \tab Example \cr
   \code{"include"} \tab \verb{"a-<a+n>" -> [a,a+n)} \tab \verb{"2020-2025" -> [2020,2025)} \cr
   \code{"exclude"} \tab \verb{"a-<a+n-1>" -> [a,a+n)} \tab \verb{"2020-2024" -> [2020,2025)} \cr
}
}

\examples{
labels <- c("2020", "<1900", "2020-2030", "2000 or more", "2030+")
period_is_open_left(labels)
period_is_open_right(labels)
}
\seealso{
\itemize{
\item \code{\link[=age_is_open_right]{age_is_open_right()}} Identify age groups open on right
\item \code{\link[=cohort_is_open_left]{cohort_is_open_left()}} Identify cohorts open on left
\item \code{\link[=cohort_is_open_right]{cohort_is_open_right()}} Identify cohorts open on right
\item \code{\link[=period_is_total]{period_is_total()}} Identify period totals
\item \code{\link[=period_is_missing]{period_is_missing()}} Identify missing period labels
\item \code{\link[=period_set_open_left]{period_set_open_left()}} Specify period open on left
\item \code{\link[=period_set_open_right]{period_set_open_right()}} Specify period open on right
}
}
