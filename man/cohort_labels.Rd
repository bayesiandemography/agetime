% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cohort_labels.R
\name{cohort_labels}
\alias{cohort_labels}
\alias{cohort_labels_one}
\alias{cohort_labels_five}
\alias{cohort_labels_ten}
\title{Create New Cohort Labels}
\usage{
cohort_labels(
  breaks,
  open = FALSE,
  format_single = c("lower", "upper"),
  format_range = c("include", "exclude"),
  include_total = FALSE,
  include_na = FALSE
)

cohort_labels_one(
  lower_first,
  lower_last,
  open = FALSE,
  format_single = c("lower", "upper"),
  format_range = c("include", "exclude"),
  include_total = FALSE,
  include_na = FALSE
)

cohort_labels_five(
  lower_first,
  lower_last,
  open = FALSE,
  format_single = c("lower", "upper"),
  format_range = c("include", "exclude"),
  include_total = FALSE,
  include_na = FALSE
)

cohort_labels_ten(
  lower_first,
  lower_last,
  open = FALSE,
  format_single = c("lower", "upper"),
  format_range = c("include", "exclude"),
  include_total = FALSE,
  include_na = FALSE
)
}
\arguments{
\item{breaks}{Boundaries between cohorts
A numeric vector.}

\item{open}{Whether first cohort
is "open", i.e. has no lower limit.
Default is \code{FALSE}.}

\item{format_single}{Rule for one-year labels: \code{"lower"} or \code{"upper"}.}

\item{format_range}{Rule for multi-year labels: \code{"include"} or \code{"exclude"}.}

\item{include_total}{Whether to include a
\code{"Total"} category.}

\item{include_na}{Whether to include
an \code{NA} category.}

\item{lower_first}{Lower limit of
first cohort.}

\item{lower_last}{Lower limit of
last cohort.}
}
\value{
Character vector.
Length depends on the function arguments.
}
\description{
Create a new set of cohort labels.
}
\examples{
## 5-year cohorts
cohort_labels_five(
  lower_first = 2000,
  lower_last = 2010
)

## 5-year cohorts, first cohort is open
cohort_labels_five(
  lower_first = 1960,
  lower_last = 2020,
  open = TRUE
)

## single-year cohorts
cohort_labels_one(
  lower_first = 2000,
  lower_last = 2010
)

## single-year cohorts, 'format_single' is "upper"
cohort_labels_one(
  lower_first = 2000,
  lower_last = 2010,
  format_single = "upper"
)

## ten-year cohorts
cohort_labels_ten(
  lower_first = 2000,
  lower_last = 2010
)

## ten-year cohorts, 'format_range' is "exclude",
cohort_labels_ten(
  lower_first = 2000,
  lower_last = 2010,
  format_range = "exclude"
)

## include total and NA
cohort_labels_ten(
  lower_first = 2000,
  lower_last = 2010,
  include_total = TRUE,
  include_na = TRUE
)
}
\seealso{
\itemize{
\item \code{\link[=age_labels]{age_labels()}} Age equivalent of \code{cohort_labels()}
\item \code{\link[=period_labels]{period_labels()}} Period equivalent of \code{cohort_labels()}
}
}
