% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/age_lower.R
\name{age_lower}
\alias{age_lower}
\alias{age_mid}
\alias{age_upper}
\alias{age_width}
\title{Limits, Widths, and Midpoints from Age Group Labels}
\usage{
age_lower(labels, interpret_fail = c("error", "warn", "silent"))

age_mid(labels, interpret_fail = c("error", "warn", "silent"))

age_upper(labels, interpret_fail = c("error", "warn", "silent"))

age_width(labels, interpret_fail = c("error", "warn", "silent"))
}
\arguments{
\item{labels}{Vector of age group labels.}

\item{interpret_fail}{Action if element of \code{labels}
cannot be interpreted. Choices are \code{"error"} (the default),
\code{"warn"}, and \code{"silent"}.}
}
\value{
Numeric vector with the same length as \code{labels}.
}
\description{
Calculate lower limits, upper limits,
widths, and midpoints of age groups.
}
\details{
Lower and upper limits can be used
to filter on age. See below for examples.

Pretending that open age groups (e.g., \code{"100+"})
have midpoints can be useful for plotting.
\code{age_mid()} assigns open age groups
midpoints based on half the median width of
the closed intervals in \code{labels}. See below for examples.
}
\examples{
labels <- c("5-9", "10-14", "100+")
age_lower(labels)
age_upper(labels)
age_width(labels)
age_mid(labels)

## use 'age_lower()' to filter on age
library(dplyr, warn.conflicts = FALSE)
df <- tribble(
  ~age, ~count,
  "5-9", 11,
  "10-14", 20,
  "100+", 7
)
df
df |> filter(age_lower(age) >= 10)

## 'midpoint' of open intervals
age_mid(c("80-89", "90-99", "100+"))
age_mid(c("90-94", "95-99", "100+"))

## no action when 'interpret_fail' is "silent"
age_lower(c("0-4", "young people", "50plus"),
  interpret_fail = "silent"
)
}
\seealso{
\itemize{
\item \code{\link[=period_lower]{period_lower()}} Period equivalent of \code{age_lower()}
\item \code{\link[=cohort_lower]{cohort_lower()}} Cohort equivalent of \code{age_lower()}
}
}
