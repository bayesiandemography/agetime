% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/age_coarsen.R
\name{age_coarsen}
\alias{age_coarsen}
\title{Coarsen Age Groups}
\usage{
age_coarsen(
  labels,
  breaks,
  open_right = NULL,
  interpret_fail = c("error", "warn", "silent")
)
}
\arguments{
\item{labels}{Vector of age group labels.}

\item{breaks}{Boundaries between age groups.
A numeric vector.}

\item{open_right}{Whether to include an age group that is
open on the right. Optional. See below for details.}

\item{interpret_fail}{Action if element of \code{labels}
cannot be interpreted. Choices are \code{"error"} (the default),
\code{"warn"}, and \code{"silent"}.}
}
\value{
Factor with the same length as \code{labels}.
}
\description{
Define new age groups that contain existing age groups.
}
\details{
The new age groups cannot split existing
age groups, and are typically wider than the existing age groups.

If \code{labels} is a factor, its levels are modified
along with its elements.
}
\section{The \code{open_right} argument}{


An age group is open on the right if it has no upper limit,
e.g. \code{"100+"}.

By default, the return value has an open age group
if and only if \code{labels} does. The full range of options is:\tabular{lll}{
   \code{open_right} \tab \code{labels} has open on right \tab Return value has open on right \cr
   \code{NULL} (default) \tab Yes \tab Yes \cr
   \code{NULL} (default) \tab No \tab No \cr
   \code{TRUE} \tab Yes \tab Yes \cr
   \code{TRUE} \tab No \tab Yes \cr
   \code{FALSE} \tab Yes \tab \emph{Error} \cr
   \code{FALSE} \tab No \tab No \cr
}
}

\examples{
labels <- c("10-14", "16", "22-23")
age_coarsen(labels, breaks = c(10, 15, 25))

## let inclusion of open age group depend on labels
labels_no_open <- c("1-4", "87-89", "0", "50-54")
age_coarsen(labels_no_open, breaks = c(0, 10, 40, 90))
labels_has_open <- c("1-4", "87+", "0", "50-54")
age_coarsen(labels_has_open, breaks = c(0, 10, 40, 87))

## insist on an open age group
age_coarsen(
  labels_no_open,
  breaks = c(0, 10, 40, 90),
  open_right = TRUE
)
}
\seealso{
\itemize{
\item \code{\link[=age_coarsen_five]{age_coarsen_five()}} Coarsen to 5-year age groups
\item \code{\link[=age_coarsen_ten]{age_coarsen_ten()}} Coarsen to 10-year age groups
\item \code{\link[=age_coarsen_life]{age_coarsen_life()}} Coarsen to life table age groups
\item \code{\link[=period_coarsen]{period_coarsen()}} Period equivalent of \code{age_coarsen()}
\item \code{\link[=cohort_coarsen]{cohort_coarsen()}} Cohort equivalent of \code{age_coarsen()}
}
}
