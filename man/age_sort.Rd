% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/age_sort.R
\name{age_sort}
\alias{age_sort}
\title{Sort Age Group Levels}
\usage{
age_sort(
  labels,
  decreasing = FALSE,
  interpret_fail = c("error", "warn", "silent")
)
}
\arguments{
\item{labels}{Vector of age group labels.}

\item{decreasing}{Whether sort is
increasing or decreasing. Default
is \code{FALSE}.}

\item{interpret_fail}{Action if element of \code{labels}
cannot be interpreted. Choices are \code{"error"} (the default),
\code{"warn"}, and \code{"silent"}.}
}
\value{
Factor with the same length as \code{labels}.
}
\description{
Sort the levels of \code{labels}.
}
\details{
If \code{labels} is not a factor, and so
does not have levels,
convert it to a factor before
sorting the levels.

Levels are sorted on their lower
limits. Upper limits are used
to resolve times. \code{NA}s come
second-to-last and totals come last.
}
\examples{
labels <- c("0-4", "50+", "Total", NA, "20-24")
age_sort(labels)
}
\seealso{
\itemize{
\item \code{\link[=period_sort]{period_sort()}} Period equivalent of \code{age_sort()}
\item \code{\link[=cohort_sort]{cohort_sort()}} Cohort equivalent of \code{age_sort()}
}
}
