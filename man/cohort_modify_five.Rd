% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cohort_modify.R
\name{cohort_modify_five}
\alias{cohort_modify_five}
\alias{cohort_modify_ten}
\title{Convert to Equal-Length Cohorts}
\usage{
cohort_modify_five(
  labels,
  offset = 0,
  interpret_single = c("lower", "upper"),
  interpret_multi = c("include", "exclude"),
  interpret_fail = c("error", "warn", "silent")
)

cohort_modify_ten(
  labels,
  offset = 0,
  interpret_single = c("lower", "upper"),
  interpret_multi = c("include", "exclude"),
  interpret_fail = c("error", "warn", "silent")
)
}
\arguments{
\item{labels}{Vector of cohort labels.}

\item{offset}{Parameter controlling
alignment of cohorts. Default is \code{0}.}

\item{interpret_single}{How to interpret
labels for single-year cohorts.
Choices are \code{"lower"} (the default) and \code{"upper"}.
See below for details.}

\item{interpret_multi}{How to interpret
labels for multi-year cohorts.
Choices are \code{"include"} (the default) and
\code{"exclude"}. See below for details.}

\item{interpret_fail}{Action if element of \code{labels}
cannot be interpreted. Choices are \code{"error"} (the default),
\code{"warn"}, and \code{"silent"}.}
}
\value{
Character vector or factor with the same length as \code{labels}.
}
\description{
Modify the cohorts used by \code{labels}.
The new cohorts must contain
the old cohorts, and follow a regular
pattern:
\itemize{
\item \code{cohort_modify_five} Five-year cohorts
\item \code{cohort_modify_ten} Ten-year cohorts
}
}
\examples{
labels <- c("2002-2004", "1987-1989", "2000", "Total")
cohort_modify_five(labels)
cohort_modify_five(labels, offset = 1)
cohort_modify_five(labels, offset = 2)
cohort_modify_ten(labels)
cohort_modify_ten(labels, offset = 1)
cohort_modify_ten(labels, offset = 2)
}
\seealso{
\itemize{
\item \code{\link[=cohort_modify]{cohort_modify()}} Convert to general cohorts
\item \code{\link[=age_modify_five]{age_modify_five()}} Age equivalent of \code{cohort_modify_five()}
\item \code{\link[=age_modify_ten]{age_modify_ten()}} Age equivalent of \code{cohort_modify_ten()}
\item \code{\link[=period_modify_five]{period_modify_five()}} Period equivalent of \code{cohort_modify_five()}
\item \code{\link[=period_modify_ten]{period_modify_ten()}} Period equivalent of \code{cohort_modify_ten()}
\item \code{\link[=cohort_levels_fill]{cohort_levels_fill()}} Add levels for intermediate cohorts
}
}
