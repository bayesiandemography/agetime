% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/age_match.R
\name{age_match}
\alias{age_match}
\alias{age_matched}
\title{Age Group Matching}
\usage{
age_match(
  x,
  table,
  type = c("equal", "inside"),
  nomatch = NA_integer_,
  invalid = c("error", "warn", "silent")
)

age_matched(
  x,
  table,
  type = c("equal", "inside"),
  invalid = c("error", "warn", "silent")
)
}
\arguments{
\item{x}{Vector of age labels}

\item{table}{Vector of age labels}

\item{type}{Type of matching. \code{"equal"} (the default)
or \code{"inside"}.}

\item{nomatch}{Value to be returned
if no match is found. Default is \code{NA_integer_}.}

\item{invalid}{Action if meaning of label
unclear. Choices are \code{"error"} (the default),
\code{"warn"}, and \code{"silent"}.}
}
\value{
\itemize{
\item \code{age_match()} An integer vector giving the position
in \code{table} of the first match if there is a match,
and \code{nomatch} otherwise.
\item \code{age_matched()} A logical vector indicating whether a match
was found for each element of \code{x}.
}
}
\description{
Matching functions for age labels.
\code{age_match()} gives the positions of first matches.
\code{age_matched()} reports whether matches were found
}
\section{The \code{type} argument}{


When \code{type} is \code{"equal"} (the default),
an element of \code{x} matches an element of \code{table}
if the element of \code{x} and the element of
\code{table} have the same upper and lower limits,
even if the labels differ. For instance,
\code{"10-14"} matches \code{"10--14"} and "10to14"`.

When \code{type} is \code{"inside"}, an element
of \code{x} matches an element of \code{table} if
the element of \code{x} is contained by the element of \code{table}.
Labels can, again, differ.
For instance, \code{"10-14"} matches \verb{"10to14" and }"10to19"`.
}

\examples{
## different labelling conventions
x <- c("0--4", "10--14", "100+")
tab <- c("0to4", "100plus")
age_match(x, tab)
age_matched(x, tab)

## 'type' argument
x <- c("10-14", "10")
tab <- "10-14"
age_matched(x, tab)
age_matched(x, tab, match = "inside")
}
\seealso{
\itemize{
\item \code{\link[=period_match]{period_match()}} \code{\link[=match]{match()}} for period labels
\item \code{\link[=cohort_match]{cohort_match()}} \code{\link[=match]{match()}} for cohort labels
\item \code{\link[=age_mapping]{age_mapping()}} mapping between age group labels
\item \code{age_matched()} is similar to base function
\link[=`\%in\%`]{\%in\%}.
}
}
