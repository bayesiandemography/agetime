% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/period_set_open_left.R,
%   R/period_set_open_right.R
\name{period_set_open_left}
\alias{period_set_open_left}
\alias{period_set_open_right}
\title{Specify Open Period}
\usage{
period_set_open_left(
  labels,
  at,
  interpret_single = c("lower", "upper"),
  interpret_multi = c("include", "exclude"),
  interpret_fail = c("error", "warn", "silent")
)

period_set_open_right(
  labels,
  at,
  interpret_single = c("lower", "upper"),
  interpret_multi = c("include", "exclude"),
  interpret_fail = c("error", "warn", "silent")
)
}
\arguments{
\item{labels}{Vector of period labels.}

\item{at}{Point at which intervals become open,
e.g. \code{2020} in \verb{<2020} or \code{2030} in \verb{2030+}.}

\item{interpret_single}{How to interpret
labels for single-year periods.
Choices are \code{"lower"} (the default) and \code{"upper"}.
See below for details.}

\item{interpret_multi}{How to interpret
labels for multi-year periods.
Choices are \code{"include"} (the default) and
\code{"exclude"}. See below for details.}

\item{interpret_fail}{Action if element of \code{labels}
cannot be interpreted. Choices are \code{"error"} (the default),
\code{"warn"}, and \code{"silent"}.}
}
\value{
Factor with the same length as \code{labels}.
}
\description{
Define a factor level representing an open period.
Replace existing periods where necessary.
\itemize{
\item \code{period_set_open_left()} defines a period with no lower limit.
\item \code{period_set_open_right()} defines a period with no upper limit.
}
}
\section{Controlling how period labels are interpreted}{


Interpretation of single-year periods:\tabular{lll}{
   \code{interpret_single} \tab Rule \tab Example \cr
   \code{"lower"} \tab \verb{"a" -> [a,a+1)} \tab \verb{"2020" -> [2020,2021)} \cr
   \code{"upper"} \tab \verb{"a" -> [a-1,a)} \tab \verb{"2020" -> [2019,2020)} \cr
}


Data providers typically use the "lower" convention
for calendar years (1 January to 31 December),
and the "upper" convention for non-calendar years
(e.g., 1 July to 30 June).

Interpretation of multi-year periods:\tabular{lll}{
   \code{interpret_multi} \tab Rule \tab Example \cr
   \code{"include"} \tab \verb{"a-<a+n>" -> [a,a+n)} \tab \verb{"2020-2025" -> [2020,2025)} \cr
   \code{"exclude"} \tab \verb{"a-<a+n-1>" -> [a,a+n)} \tab \verb{"2020-2024" -> [2020,2025)} \cr
}
}

\examples{
labels <- c("2020-2024", "2025-2029")
period_set_open_left(labels, at = 2020)

labels <- c("2020-2024", "2025-2029", "2030")
period_set_open_right(labels, at = 2030)
}
\seealso{
\itemize{
\item \code{\link[=age_set_open_right]{age_set_open_right()}} Specify age group open on right
\item \code{\link[=cohort_set_open_left]{cohort_set_open_left()}} Specify cohort open on left
\item \code{\link[=cohort_set_open_right]{cohort_set_open_right()}} Specify cohort open on right
\item \code{\link[=period_is_open_left]{period_is_open_left()}} Identify periods open on left
\item \code{\link[=period_is_open_right]{period_is_open_right()}} Identify periods open on right
}
}
