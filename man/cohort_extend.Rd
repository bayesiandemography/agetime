% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cohort_extend.R
\name{cohort_extend}
\alias{cohort_extend}
\title{Extend a Set of Cohorts}
\usage{
cohort_extend(
  labels,
  n = 1L,
  width = NULL,
  include_x = TRUE,
  interpret_single = c("lower", "upper"),
  interpret_multi = c("include", "exclude"),
  interpret_fail = c("error", "warn", "silent")
)
}
\arguments{
\item{labels}{Vector of cohort labels.}

\item{n}{Number of cohorts to add.
Default is \code{1}.}

\item{width}{Width of the cohorts
to be added.}

\item{include_x}{Should the return value
include \code{labels}? Default is \code{TRUE}.}

\item{interpret_single}{How to interpret
labels for single-year cohorts.
Choices are \code{"lower"} (the default) and \code{"upper"}.
See below for details.}

\item{interpret_multi}{How to interpret
labels for multi-year cohorts.
Choices are \code{"include"} (the default) and
\code{"exclude"}. See below for details.}

\item{interpret_fail}{Action if element of \code{labels}
cannot be interpreted. Choices are \code{"error"} (the default),
\code{"warn"}, and \code{"silent"}.}
}
\value{
Character vector or factor.
}
\description{
Continue an existing set of cohort labels.
}
\details{
By default, the width of the new cohorts
is derived from the last element of \code{labels},
but a value can be specified through the
\code{width} arugment.
}
\section{Controlling how cohort labels are interpreted}{


Interpretation of single-year cohorts:\tabular{lll}{
   \code{interpret_single} \tab Rule \tab Example \cr
   \code{"lower"} \tab \verb{"a" -> [a,a+1)} \tab \verb{"2020" -> [2020,2021)} \cr
   \code{"upper"} \tab \verb{"a" -> [a-1,a)} \tab \verb{"2020" -> [2019,2020)} \cr
}


Data providers typically use the "lower" convention
for calendar years (1 January to 31 December),
and the "upper" convention for non-calendar years
(e.g., 1 July to 30 June).

Interpretation of multi-year cohorts:\tabular{lll}{
   \code{interpret_multi} \tab Rule \tab Example \cr
   \code{"include"} \tab \verb{"a-<a+n>" -> [a,a+n)} \tab \verb{"2020-2025" -> [2020,2025)} \cr
   \code{"exclude"} \tab \verb{"a-<a+n-1>" -> [a,a+n)} \tab \verb{"2020-2024" -> [2020,2025)} \cr
}
}

\examples{
labels <- c("2020-2025", "2025-2030")
cohort_extend(labels, n = 2)
cohort_extend(labels, n = 2, width = 10)
cohort_extend(labels, n = 2, include_x = FALSE)
}
\seealso{
\itemize{
\item \code{\link[=age_extend]{age_extend()}} Age equivalent of \code{cohort_extend()}
\item \code{\link[=period_extend]{period_extend()}} Period equivalent of \code{cohort_extend()}
}
}
