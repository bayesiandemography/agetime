% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/age_extend.R
\name{age_extend}
\alias{age_extend}
\title{Extend a Set of Age Groups}
\usage{
age_extend(
  x,
  n = 1L,
  width = NULL,
  include_x = TRUE,
  parse_fail = c("error", "warn", "silent")
)
}
\arguments{
\item{x}{Vector of age group labels.}

\item{n}{The number of age groups to add.
Default is \code{1}.}

\item{include_x}{Should the return value
include \code{x}? Default is \code{TRUE}.}

\item{parse_fail}{Action if meaning of label
unclear. Choices are \code{"error"} (the default),
\code{"warn"}, and \code{"silent"}.}
}
\value{
If \code{x} is a factor, \code{age_extend}
returns a factor; otherwise it returns
a character vector.
}
\description{
Add \code{n} age groups to an existing set of labels \code{x}.
The width of the age groups is derived from the
\code{width} argument, or from the width of the last
label in \code{x}.
}
\examples{
x <- c("0-4", "5-9")
age_extend(x, n = 2)
age_extend(x, n = 2, width = 10)
age_extend(x, n = 2, include_x = FALSE)
}
