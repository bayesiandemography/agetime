% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/age_extend.R
\name{age_extend}
\alias{age_extend}
\title{Extend a Set of Age Groups}
\usage{
age_extend(
  labels,
  n = 1L,
  width = NULL,
  include_x = TRUE,
  interpret_fail = c("error", "warn", "silent")
)
}
\arguments{
\item{labels}{Vector of age group labels.}

\item{n}{Number of age groups to add.
Default is \code{1}.}

\item{width}{Width of the age groups
to be added.}

\item{include_x}{Should the return value
include \code{labels}? Default is \code{TRUE}.}

\item{interpret_fail}{Action if element of \code{labels}
cannot be interpreted. Choices are \code{"error"} (the default),
\code{"warn"}, and \code{"silent"}.}
}
\value{
Character vector or factor.
}
\description{
Continue an existing set of age group labels.
}
\details{
By default, the width of the new age groups
is derived from the last element of \code{labels},
but a value can be specified through the
\code{width} arugment.
}
\examples{
labels <- c("0-4", "5-9")
age_extend(labels, n = 2)
age_extend(labels, n = 2, width = 10)
age_extend(labels, n = 2, include_x = FALSE)
}
\seealso{
\itemize{
\item \code{\link[=period_extend]{period_extend()}} Period equivalent of \code{age_extend()}
\item \code{\link[=cohort_extend]{cohort_extend()}} Cohort equivalent of \code{age_extend()}
}
}
