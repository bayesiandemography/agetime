% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cohort_is_total.R
\name{cohort_is_total}
\alias{cohort_is_total}
\title{Identify Cohort Labels that Refer to Totals}
\usage{
cohort_is_total(
  x,
  x_one = c("lower", "upper"),
  x_multi = c("include", "exclude"),
  x_fail = c("error", "warn", "silent")
)
}
\arguments{
\item{x}{A vector of cohort labels.}

\item{x_one}{Whether labels
for one-year cohorts are based on the
lower or upper limit of the period.
Default is \code{"lower"}.}

\item{x_multi}{Whether
labels for multi-year periods
include or exclude the final
year of the period.
Default is \code{"include"}.}

\item{x_fail}{Action if a label
cannot be interpreted. Choices are
\code{"error"} (the default), \code{"warn"},
and \code{"silent"}.}
}
\value{
A logical vector the same length as \code{x}.
}
\description{
Find cohort labels that \pkg{agetime} interprets as totals.
}
\examples{
x <- c("2020-2025", "Total", "1999", "ALL")
cohort_is_total(x)
}
