% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cohort_modify.R
\name{cohort_modify}
\alias{cohort_modify}
\title{Convert to New Cohorts}
\usage{
cohort_modify(
  labels,
  breaks,
  open_left = NULL,
  open_right = NULL,
  interpret_single = c("lower", "upper"),
  interpret_multi = c("include", "exclude"),
  interpret_fail = c("error", "warn", "silent")
)
}
\arguments{
\item{labels}{Vector of cohort labels.}

\item{breaks}{Boundaries between cohorts.
A numeric vector.}

\item{open_left}{Whether the first cohort
is open on the left, i.e. has no lower limit.
Default is \code{NULL}, which infers from \code{labels}:
\code{TRUE} when any label is open on the left, otherwise \code{FALSE}.
Use \code{TRUE} or \code{FALSE} to add or suppress an open left level
regardless of \code{labels}.}

\item{open_right}{Whether the last cohort
is open on the right, i.e. has no upper limit.
Default is \code{NULL}, which infers from \code{labels}:
\code{TRUE} when any label is open on the right, otherwise \code{FALSE}.
Use \code{TRUE} or \code{FALSE} to add or suppress an open right level
regardless of \code{labels}.}

\item{interpret_single}{How to interpret
labels for single-year cohorts.
Choices are \code{"lower"} (the default) and \code{"upper"}.
See below for details.}

\item{interpret_multi}{How to interpret
labels for multi-year cohorts.
Choices are \code{"include"} (the default) and
\code{"exclude"}. See below for details.}

\item{interpret_fail}{Action if element of \code{labels}
cannot be interpreted. Choices are \code{"error"} (the default),
\code{"warn"}, and \code{"silent"}.}
}
\value{
Factor with the same length as \code{labels}.
}
\description{
Modify the cohorts used by \code{labels}. The
the new cohorts must
contain the old ones.
}
\section{Controlling how cohort labels are interpreted}{


If \code{interpret_single} is \code{"lower"} (the default),
then labels for single-year cohorts
are assumed to refer to lower limits,
so that \code{"2025"} means \verb{[2025,2026)}.
This is the convention that data providers
typically use for calendar years.

If \code{interpret_single} is \code{"upper"},
then labels for single-year cohorts
are assumed to refer to upper limits,
so that \code{"2025"} means \verb{[2024,2025)}.
This is the convention that data providers
typically use for non-calendar years,
such as 1 July to 30 June.

If \code{interpret_multi} is \code{"include"} (the default),
then labels for multi-year cohorts
are assumed to include upper limits,
so that \code{"2025-2030"} means \verb{[2025,2030)}.

If \code{interpret_multi} is \code{"exclude"},
then labels for multi-year cohorts
are assumed to exclude the upper limits
so that \code{"2025-2030"} means \verb{[2025,2031)}.
}

\examples{
labels <- c("2001-2004", "1987-1989", "2000", "2005-2010")
cohort_modify(labels, breaks = c(1970, 2000, 2005, 2015))
cohort_modify(labels, breaks = c(1970, 2000, 2005, 2015), open_left = TRUE)

}
\seealso{
\itemize{
\item \code{\link[=cohort_modify_five]{cohort_modify_five()}} Convert to 5-year cohorts
\item \code{\link[=cohort_modify_ten]{cohort_modify_ten()}} Convert to 10-year cohorts
\item \code{\link[=age_modify]{age_modify()}} Age group equivalent of \code{cohort_modify()}
\item \code{\link[=period_modify]{period_modify()}} Period equivalent of \code{cohort_modify()}
}
}
