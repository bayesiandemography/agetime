% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cohort_modify.R
\name{cohort_modify}
\alias{cohort_modify}
\title{Convert to New Cohorts}
\usage{
cohort_modify(
  labels,
  breaks,
  open = FALSE,
  interpret_single = c("lower", "upper"),
  interpret_range = c("include", "exclude"),
  interpret_fail = c("error", "warn", "silent")
)
}
\arguments{
\item{labels}{Vector of cohort labels.}

\item{breaks}{Boundaries between cohorts.
A numeric vector.}

\item{open}{Whether the first cohort
is "open", i.e. has no lower limit.
Default is \code{FALSE}.}

\item{interpret_single}{How to interpret a single-year label.
If \code{"lower"} (the default), the label is assumed to
give the lower bound, so that \code{"2025"} means\verb{[2025,2026)}.
If \code{"upper"}, the label is assumed to give the upper bound,
so that \code{"2025"} means \verb{[2024,2025)}.}

\item{interpret_range}{How to interpret a label describing a range.
If \code{"include"} (the default), the label is assumed to give
the lower and upper bounds, so that \code{"2025-2030"}
means \verb{[2025, 2030)}.
If \code{"exclude"}, the label is assumed to give the lower
bound and the upper bound minus 1, so that \code{"2025-2030"}
means \verb{[2025, 2031)}.}

\item{interpret_fail}{What to do if an attempt to interpret a
label fails: \code{"error"} (the default), \code{"warn"}, or \code{"silent"}.}
}
\value{
Character vector or factor with the same length as \code{labels}.
}
\description{
Modify the cohorts used by \code{labels}. The
the new cohorts must
contain the old ones.
}
\examples{
labels <- c("2001-2004", "1987-1989", "2000", "2005-2010")
cohort_modify(labels, breaks = c(1970, 2000, 2005, 2015))
cohort_modify(labels, breaks = c(1970, 2000, 2005, 2015), open = TRUE)

}
\seealso{
\itemize{
\item \code{\link[=cohort_modify_five]{cohort_modify_five()}} Convert to 5-year cohorts
\item \code{\link[=cohort_modify_ten]{cohort_modify_ten()}} Convert to 10-year cohorts
\item \code{\link[=age_modify]{age_modify()}} Age group equivalent of \code{cohort_modify()}
\item \code{\link[=period_modify]{period_modify()}} Period equivalent of \code{cohort_modify()}
}
}
