% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cohort_modify.R
\name{cohort_modify}
\alias{cohort_modify}
\title{Convert to New Cohorts}
\usage{
cohort_modify(
  x,
  breaks,
  open = FALSE,
  x_one = c("lower", "upper"),
  x_multi = c("include", "exclude"),
  x_fail = c("error", "warn", "silent")
)
}
\arguments{
\item{x}{A vector of cohort labels.}

\item{breaks}{Boundaries between cohorts.
A numeric vector.}

\item{open}{Whether the first cohort
is "open", i.e. has no lower limit.
Default is \code{FALSE}.}

\item{x_one}{Whether labels
for one-year cohorts are based on the
lower or upper limit of the period.
Default is \code{"lower"}.}

\item{x_multi}{Whether
labels for multi-year periods
include or exclude the final
year of the period.
Default is \code{"include"}.}

\item{x_fail}{Action if a label
cannot be interpreted. Choices are
\code{"error"} (the default), \code{"warn"},
and \code{"silent"}.}
}
\value{
If \code{x} is a factor, then the return value is
a factor; otherwise it is a character vector.
}
\description{
Modify the cohorts used by \code{x}. The
the new cohorts must
contain the old ones.
}
\examples{
x <- c("2001-2004", "1987-1989", "2000", "2005-2010")
cohort_modify(x, breaks = c(1970, 2000, 2005, 2015))
cohort_modify(x, breaks = c(1970, 2000, 2005, 2015), open = TRUE)
}
\seealso{
\code{\link[=cohort_modify_five]{cohort_modify_five()}} Convert to 5-year cohorts
\code{\link[=cohort_modify_ten]{cohort_modify_ten()}} Convert to 10-year cohorts
\code{\link[=age_modify]{age_modify()}} Age group equivalent of \code{cohort_modify()}
\code{\link[=period_modify]{period_modify()}} Period equivalent of \code{cohort_modify()}
}
